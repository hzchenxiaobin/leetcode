# 每日温度

- **题目名称**：每日温度
- **链接**：[739. 每日温度](https://leetcode.cn/problems/daily-temperatures/)
- **难度**：中等
- **标签**：数组、单调栈

## 1. 题目概述

给定一个整数数组 `temperatures`，表示每天的温度。返回一个数组 `answer`，其中 `answer[i]` 是指对于第 `i` 天，下一个更高温度出现在几天后。如果此后没有更高的温度，则 `answer[i] == 0`。

**示例 1**：

```text
输入：temperatures = [73,74,75,71,69,72,76,73]
输出：[1,1,4,2,1,1,0,0]
解释：
  第 0 天 73° → 第 1 天 74° 更高，隔 1 天 → answer[0]=1
  第 2 天 75° → 第 6 天 76° 更高，隔 4 天 → answer[2]=4
  第 6 天 76° → 后面没有更高 → answer[6]=0
```

**示例 2**：

```text
输入：temperatures = [30,40,50,60]
输出：[1,1,1,0]
```

**示例 3**：

```text
输入：temperatures = [30,60,90]
输出：[1,1,0]
```

**约束条件**：

- `1 <= temperatures.length <= 10^5`
- `30 <= temperatures[i] <= 100`

> 💡 这是 **单调栈（monotonic stack）** 的招牌题，与 [Week1/Day7 柱状图最大矩形](../../week1/day7/柱状图中最大的矩形.md) 是同一套模板的两种应用。柱状图找"左右第一个更矮"，本题找"**右边第一个更高**"——核心都是用栈维护一个**单调序列**，让"找下一个更大/更小"从 `O(n²)` 暴力降到 `O(n)`。掌握这个模板，就能秒杀下一个更大元素、接雨水、股票跨度等所有"找下一个更 X"问题。

---

## 2. 解题思路

### 2.1 暴力思路：双重循环

对每个 `i`，向后扫描找到第一个 `j > i` 使 `temperatures[j] > temperatures[i]`，`answer[i] = j - i`。

```text
for i in 0..n:
    for j in i+1..n:
        if temperatures[j] > temperatures[i]:
            answer[i] = j - i
            break
```

时间复杂度 `O(n²)`，`n = 10^5` 时约 `10^10` 次运算，**超时**。

> ⚠️ 暴力法的问题：每个元素向后扫描时**重复比较了大量已扫过的元素**。例如 `[75,71,69,72,76]`，找 75 的下一个更大时扫过 71、69、72、76；找 71 的下一个更大又扫过 69、72……。这些重复比较可以通过**单调栈**消除——栈里记住"还没找到答案的元素"，一旦遇到更大的就一次性结算多个答案。

### 2.2 核心观察：单调递减栈

**关键定义**：维护一个**栈**，栈中存放下标，对应温度从栈底到栈顶**单调递减**。

**核心规则**：遍历每天温度 `temperatures[i]`：

- 若 `temperatures[i]` **大于**栈顶对应温度：说明第 `i` 天就是栈顶那天的"下一个更高温度"！弹出栈顶 `top`，`answer[top] = i - top`。**循环弹**直到栈顶温度 ≥ 当前温度（保持单调性）或栈空。
- 然后**把 `i` 入栈**（它还没找到答案，等后续更大的来结算）。

![单调递减栈：遇到更大温度时连续弹栈结算](../../../images/daily_temperatures_monotonic_stack.svg)

**为什么是递减栈？** 因为我们要找"下一个更大"。栈里存的是"还没找到更大值"的元素，它们应该**递减**排列——如果栈里出现 `a < b`（栈顶附近有较小者在较大者下方），那 `b` 的答案先被更大的值结算，`a` 的答案在 `b` 之后。递减保证栈顶总是"最迫切等待更大值"的那个。

> 💡 **单调栈的精髓**：栈不是"存储所有元素"，而是"**暂存尚未结算的元素**"。每个元素入栈 1 次、出栈 1 次，总操作 `O(n)`，把暴力的 `O(n²)` 降到 `O(n)`。这是"用空间换时间"的经典范式。

### 2.3 算法流程图

![算法流程：遍历 → 比栈顶大则弹栈结算 → 入栈](../../../images/daily_temperatures_algorithm_flow.svg)

**完整步骤**：

1. **初始化**：栈 `stack = []`，结果 `answer` 全填 0
2. **遍历 `i` 从 0 到 n-1**：
   - `while stack 非空 且 temperatures[i] > temperatures[stack.top()]`：
     - `top = stack.pop()`
     - `answer[top] = i - top`（第 `top` 天的答案是 `i - top`）
   - `stack.push(i)`
3. **结束**：栈中剩余元素 `answer` 保持 0（后面没有更高温度）

> ⚠️ 栈里存**下标**而非温度值本身——因为 `answer[i]` 要算"隔几天"（下标差），存下标才能算 `i - top`。这是单调栈题的通用技巧。

### 2.4 示例演算

以 `temperatures = [73,74,75,71,69,72,76,73]` 为例：

![示例演算：8 天温度的单调栈变化](../../../images/daily_temperatures_example_walkthrough.svg)

| 步骤 | i | 温度 | 栈（下标） | 动作 | answer 变化 |
|------|---|------|-----------|------|------------|
| 1 | 0 | 73 | [] | 栈空，入栈 | [0,0,0,0,0,0,0,0] |
| 2 | 1 | 74 | [0] | 74>73，弹 0，answer[0]=1-0=1；入栈 1 | [1,0,0,0,0,0,0,0] |
| 3 | 2 | 75 | [1] | 75>74，弹 1，answer[1]=2-1=1；入栈 2 | [1,1,0,0,0,0,0,0] |
| 4 | 3 | 71 | [2] | 71<75，不弹，入栈 3 | [1,1,0,0,0,0,0,0] |
| 5 | 4 | 69 | [2,3] | 69<71，不弹，入栈 4 | [1,1,0,0,0,0,0,0] |
| 6 | 5 | 72 | [2,3,4] | 72>69 弹4→ans[4]=1；72>71 弹3→ans[3]=2；72<75 停；入栈5 | [1,1,0,2,1,0,0,0] |
| 7 | 6 | 76 | [2,5] | 76>72 弹5→ans[5]=1；76>75 弹2→ans[2]=4；栈空；入栈6 | [1,1,4,2,1,1,0,0] |
| 8 | 7 | 73 | [6] | 73<76，不弹，入栈 7 | [1,1,4,2,1,1,0,0] |
| 结束 | — | — | [6,7] | 剩余 answer 保持 0 | [1,1,4,2,1,1,0,0] |

注意步骤 6 和 7：一个新温度进来可能**连续弹多个**栈顶（72 弹了 4 和 3，76 弹了 5 和 2），这正是单调栈"一次结算多个答案"的威力。步骤 8 后栈剩 `[6,7]`（76 和 73），它们后面没有更高温度，answer 保持 0。

> 💡 对比暴力法：找 75（下标 2）的答案，暴力要从下标 3 扫到 6（扫 4 次）；单调栈在第 6 步（i=6, 温度 76）时一次性弹掉栈里的 5 和 2，75 的答案直接算出 `6-2=4`。每个元素只被弹 1 次，总操作 `O(n)`。

---

## 3. 参考代码

### C++

```cpp
// 每日温度.cpp —— 单调递减栈
// 编译: g++ -O2 -std=c++17 每日温度.cpp -o dailytemp
#include <vector>
#include <stack>
using namespace std;

class Solution {
  public:
    vector<int> dailyTemperatures(vector<int>& temperatures) {
        int n = temperatures.size();
        vector<int> answer(n, 0); // 默认 0（无更高温度）
        stack<int> stk;           // 存下标，对应温度单调递减

        for (int i = 0; i < n; ++i) {
            // 当前温度比栈顶高 → 结算栈顶
            while (!stk.empty() && temperatures[i] > temperatures[stk.top()]) {
                int top = stk.top();
                stk.pop();
                answer[top] = i - top; // 隔 i - top 天
            }
            stk.push(i); // 当前入栈，等后续更大的来结算
        }
        // 栈中剩余元素 answer 已是 0，无需处理
        return answer;
    }
};
```

### Python

```python
class Solution:
    def dailyTemperatures(self, temperatures: list[int]) -> list[int]:
        n = len(temperatures)
        answer = [0] * n
        stack = []                          # 存下标，温度单调递减

        for i, t in enumerate(temperatures):
            # 当前温度比栈顶高 → 结算栈顶
            while stack and t > temperatures[stack[-1]]:
                top = stack.pop()
                answer[top] = i - top
            stack.append(i)
        # 栈中剩余 answer 已是 0
        return answer
```

> 💡 Python 用 `list` 当栈（`append` 入栈，`pop()` 出栈，均 `O(1)`）。`stack[-1]` 是栈顶下标，`temperatures[stack[-1]]` 是栈顶温度。

---

## 4. 复杂度分析

| 维度 | 复杂度 | 说明 |
|------|--------|------|
| **时间复杂度** | `O(n)` | 每个下标恰好入栈 1 次、出栈 1 次，总操作 `2n` |
| **空间复杂度** | `O(n)` | 栈最多存 `n` 个下标（温度严格递减时） |

> ⚠️ 虽然有 `while` 嵌套在 `for` 里，看似 `O(n²)`，但**均摊分析**：每个元素至多入栈 1 次出栈 1 次，`while` 的总弹出次数 ≤ `n`，所以总时间 `O(n)`。这是单调栈"均摊 O(1)"的特性。

---

## 5. 扩展：单调栈的通用模板与变体

### 5.1 通用模板

"找下一个更 X"类问题的统一骨架：

```python
stack = []
for i in range(n):
    while stack and 比较条件(arr[i], arr[stack[-1]]):
        top = stack.pop()
        # 结算 top 的答案，基于 i
    stack.append(i)
# 栈中剩余元素无"下一个更 X"，answer 保持默认（0 或 -1）
```

**比较条件**决定单调性：

| 找什么 | 比较条件 | 栈单调性 |
|--------|---------|---------|
| 下一个更大（本题） | `arr[i] > arr[stack[-1]]` | 递减栈 |
| 下一个更小 | `arr[i] < arr[stack[-1]]` | 递增栈 |
| 下一个更大或等于 | `arr[i] >= arr[stack[-1]]` | 严格递减 |

### 5.2 相关变体题

| 题目 | 找什么 | 与本题差异 |
|------|--------|-----------|
| 496 下一个更大元素 I | 下一个更大 | 两个数组，需哈希映射值到下标 |
| 503 下一个更大元素 II | 下一个更大（循环数组） | 拼接两遍数组或用取模 |
| 84 柱状图最大矩形 | 左右第一个更矮 | 两侧各扫一遍，递增栈 |
| 42 接雨水 | 左右最大值 | 单调栈或双指针均可 |
| 901 股票价格跨度 | 左边连续更小或等 | 单调递减栈，弹栈时累加跨度 |

> 💡 这些题的骨架**完全相同**——都是"遍历 + while 弹栈结算 + 入栈"。差别仅在比较方向（更大/更小）、结算内容（算距离/算面积/累加跨度）、扫描方向（单侧/双侧）。掌握每日温度的模板，496/503 可秒杀，84/42 的单调栈解法也能快速理解。

### 5.3 与双指针/DP 的对比

部分"找下一个更大"问题也能用别的解法：

- **接雨水（42）**：单调栈、双指针、DP 三种解法，单调栈最直观但双指针省空间
- **股票跨度（901）**：单调栈最优，DP 退化到 `O(n²)`
- **每日温度**：单调栈是标准解，无更优替代

> 💡 单调栈适合"**找下一个更 X 且需要结算多个元素**"的场景。若只需找一侧最大值（如接雨水的 `left_max`），DP/前缀最大更简单。面试时按问题特征选择。

---

## 6. 面试要点

1. **为什么用单调栈而不是双指针或 DP？**

   - 本题要找"每个元素的下一个更大"，**双向**依赖（一个元素可能被前面多个元素等待结算）。双指针适合"两侧夹逼"的单向问题，不适合这种"一对多结算"。
   - DP 需要无后效性的状态定义，但"下一个更大"依赖**未来**的元素，反向 DP 虽可行但不如单调栈直观。
   - 单调栈天然适合"暂存未结算元素，遇到触发条件批量结算"的模式，是本题的最优解。

2. **栈里存下标还是存值？为什么？**

   - 存**下标**。因为 `answer[i]` 要算"隔几天"（`i - top`），必须知道下标差。
   - 存值只能知道"下一个更大的温度是多少"，算不出"隔几天"。
   - 通用原则：需要计算**距离/位置差**时存下标，只需比较值时存值（少见）。

3. **为什么是单调递减栈？递增栈行不行？**

   - 找"下一个更大"，栈里存"还没找到更大的"元素。若栈递增（栈顶最大），新来的小元素入栈后，栈顶的小元素被压在下面，永远等不到更大的来结算——破坏了"栈顶先结算"的机制。
   - 递减栈保证栈顶是最小的、最迫切等更大值的元素，新来的值先和它比，满足"先进后出"的结算顺序。
   - 口诀：**找更大用递减栈，找更小用递增栈**。

4. **时间复杂度为什么是 O(n) 而不是 O(n²)？**

   - 虽然 `while` 嵌套在 `for` 里，但**均摊分析**：每个元素入栈 1 次、出栈 1 次，`while` 的总弹出次数 ≤ `n`。
   - `for` 跑 `n` 轮，每轮的 `while` 总共弹出 `≤ n` 次（不是每轮都弹 `n` 次），所以总操作 `O(n) + O(n) = O(n)`。
   - 这是"每个元素均摊 O(1)"的经典例子，与 [Day 4 合并链表](../day4/合并两个有序链表.md) 的"每节点 O(1)"类似。

5. **栈中剩余元素为什么 answer 是 0？**

   - 遍历结束后栈里剩的是"右边没有更高温度"的元素（如示例的 76、73）。
   - `answer` 初始化全 0，这些元素从未被结算，保持 0，正好符合"此后没有更高温度则 answer[i]=0"的题意。
   - 不需要额外处理栈中剩余——这是初始化为 0 的妙处。

> 💡 **一句话总结**：每日温度是单调栈的招牌题——它用"递减栈暂存未结算元素，遇到更大值批量弹栈结算"的模式，把"找下一个更大"从 `O(n²)` 降到 `O(n)`。核心技巧是**存下标而非值**（算距离需要）和**递减栈**（找更大用递减）。这个 `while + pop + push` 骨架可迁移到下一个更大元素、柱状图最大矩形、接雨水、股票跨度等所有"找下一个更 X"问题，是面试必会的核心模板。

---

## 7. 同类练习题
- [496. 下一个更大元素 I](https://leetcode.cn/problems/next-greater-element-i/)：单调栈入门
- [503. 下一个更大元素 II](https://leetcode.cn/problems/next-greater-element-ii/)：环形数组单调栈
- [42. 接雨水](https://leetcode.cn/problems/trapping-rain-water/)：单调栈经典应用
