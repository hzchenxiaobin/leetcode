<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 520 360" font-family="'Comic Sans MS', 'Segoe UI', 'Kaiti SC', 楷体, cursive">
  <defs>
    <filter id="rough">
      <feTurbulence type="fractalNoise" baseFrequency="0.02" numOctaves="2" seed="3"/>
      <feDisplacementMap in="SourceGraphic" scale="2"/>
    </filter>
    <filter id="rough2">
      <feTurbulence type="fractalNoise" baseFrequency="0.025" numOctaves="2" seed="7"/>
      <feDisplacementMap in="SourceGraphic" scale="1.5"/>
    </filter>
    <marker id="arr" markerWidth="8" markerHeight="8" refX="6" refY="4" orient="auto">
      <path d="M0,0 L7,4 L0,8 z" fill="#446688"/>
    </marker>
  </defs>

  <rect width="520" height="360" fill="#fafafa"/>

  <text x="260" y="24" text-anchor="middle" font-size="14" fill="#555" font-weight="bold">自底向上 DP 算法流程</text>

  <!-- Step 1: init -->
  <rect x="160" y="44" width="200" height="40" fill="#e8f0fe" stroke="#446688" stroke-width="1.5" rx="8" filter="url(#rough2)"/>
  <text x="260" y="62" text-anchor="middle" font-size="12" fill="#333" font-weight="bold">① dp ← triangle 最后一行</text>
  <text x="260" y="78" text-anchor="middle" font-size="10" fill="#888">dp[j] = triangle[n−1][j]</text>

  <path d="M 260 86 L 260 104" stroke="#446688" stroke-width="1.5" fill="none" marker-end="url(#arr)" filter="url(#rough2)"/>

  <!-- Step 2: loop header (diamond) -->
  <polygon points="260,108 360,148 260,188 160,148" fill="#fff8e1" stroke="#d6a040" stroke-width="1.5" filter="url(#rough2)"/>
  <text x="260" y="146" text-anchor="middle" font-size="12" fill="#333" font-weight="bold">i 从 n−2 到 0 ？</text>
  <text x="260" y="162" text-anchor="middle" font-size="10" fill="#888">倒序遍历每一行</text>

  <!-- Yes branch down -->
  <path d="M 260 188 L 260 206" stroke="#446688" stroke-width="1.5" fill="none" marker-end="url(#arr)" filter="url(#rough2)"/>
  <text x="270" y="200" font-size="10" fill="#4a7a3a">是</text>

  <!-- Step 3: inner loop -->
  <rect x="140" y="210" width="240" height="50" fill="#e6f4ea" stroke="#4a7a3a" stroke-width="1.5" rx="8" filter="url(#rough2)"/>
  <text x="260" y="230" text-anchor="middle" font-size="12" fill="#333" font-weight="bold">② for j = 0 .. i：</text>
  <text x="260" y="250" text-anchor="middle" font-size="11" fill="#444">dp[j] = min(dp[j], dp[j+1]) + triangle[i][j]</text>

  <!-- loop back arrow to diamond -->
  <path d="M 380 235 L 410 235 L 410 148 L 362 148" stroke="#446688" stroke-width="1.2" fill="none" stroke-dasharray="4,2" marker-end="url(#arr)" filter="url(#rough2)"/>
  <text x="404" y="195" font-size="10" fill="#888">j++ / i−−</text>

  <!-- No branch left -->
  <path d="M 160 148 L 90 148 L 90 290" stroke="#b85450" stroke-width="1.5" fill="none" marker-end="url(#arr)" filter="url(#rough2)"/>
  <text x="120" y="142" font-size="10" fill="#b85450">否</text>

  <!-- Step 4: return -->
  <rect x="40" y="288" width="180" height="40" fill="#fce4ec" stroke="#b85450" stroke-width="2" rx="8" filter="url(#rough2)"/>
  <text x="130" y="306" text-anchor="middle" font-size="12" fill="#333" font-weight="bold">③ 返回 dp[0]</text>
  <text x="130" y="322" text-anchor="middle" font-size="10" fill="#888">即顶到底最小路径和</text>

  <!-- Note box -->
  <rect x="270" y="288" width="210" height="50" fill="#fff8e1" stroke="#d6a040" stroke-width="1.5" rx="10" filter="url(#rough2)"/>
  <text x="375" y="308" text-anchor="middle" font-size="11" fill="#444">关键：倒序行 + 顺序列</text>
  <text x="375" y="324" text-anchor="middle" font-size="10" fill="#888">dp[j+1] 是「右下子」，本轮尚未覆盖</text>
</svg>
