---
name: leetcode-daily
description: 用于在 leetcode/solution 下编写每日 LeetCode 题解。规定了按序号补全的选题逻辑、目录组织、题解文档结构、手绘 sketch 风 SVG 插图规范与网站构建集成。触发于"写 leetcode 每日题解"、"补全 solution 题解"、"按序号补缺题"、"加一道 leetcode 题解"等请求。
---

# 写 LeetCode Daily 题解 Skill

本工程(`ai-infra-notes`)的 LeetCode 每日题解遵循一套固定的目录组织、文档结构和插图规范。本 skill 描述如何产出符合仓库惯例的题解。

## 1. 选题逻辑：按序号补全

每日题解的目标是**按题号顺序逐题补全，形成题号 1–N 的连续覆盖**，而非按面试频率挑选。选题时遵循以下原则。

### 1.1 选题原则

| 原则 | 说明 |
|------|------|
| **按序号顺序** | 从最小缺失题号开始补，逐题填写，不跳号；下一个要写的题号见 1.3 清单的首行 |
| **逐区间推进** | 先补完 `0001-0100` 区间的所有缺题，再进入 `0101-0200`，依此类推；区间目录与题号映射见第 2 节 |
| **难度不挑选** | 不按难度筛选，序号在前即先写（简单/中等/困难混合推进） |
| **标签不挑选** | 不按算法标签筛选，序号优先；1.2 标签索引仅作已写题解的分类参考，不驱动选题 |
| **题目不重复** | 同一道题在整个 solution 系列中只出现一次；选题前先查 `leetcode/solution/` 已有文件与 1.3 缺题清单 |
| **完整覆盖** | 目标是题号 1–N 的无缺口连续覆盖；周赛题解（`contest/`）不在本序列内，不参与补全 |

### 1.2 已写题解的标签索引（参考用）

下表按标签分组列出**已写题解中具有代表性的题目**，作为按标签检索题解的索引。⚠️ 此表仅覆盖部分已写题解（全站共 362 篇，表中列出的是面试高频维度的精选子集），**不再驱动选题**——选题完全按 1.3 缺题清单的序号顺序进行。所有题目状态均为 ✅（已写题解）。

#### 数组 / 字符串 / 滑动窗口

| 题号 | 题目 | 难度 | 核心套路 | 状态 |
|------|------|------|----------|------|
| 912 | 排序数组 | 中等 | 手撕快排 / 归并 / 堆排 | ✅ |
| 26 | 删除有序数组中的重复项 | 简单 | 快慢双指针 + slow−1 判定（80 题基础版，回看 1 格，通用模板 K=1） | ✅ |
| 27 | 移除元素 | 简单 | 快慢双指针 + 与 val 直接比较（无序删指定值，对照 26 删重复值；可换序→左右双指针交换） | ✅ |
| 80 | 删除有序数组中的重复项 II | 中等 | 快慢双指针 + slow−2 判定 | ✅ |
| 415 | 字符串相加 | 简单 | 大数加法模拟进位 | ✅ |
| 66 | 加一 | 简单 | 数组逐位进位（遇 9 置 0 进位，遇非 9 即停，415 退化版） | ✅ |
| 67 | 二进制求和 | 简单 | 大数加法模拟进位（基数为 2，sum&amp;1 写位 / sum&gt;&gt;1 进位，承接 415/66） | ✅ |
| 165 | 比较版本号 | 中等 | 字符串切分 + 逐段比较 | ✅ |
| 8 | 字符串转换整数（atoi） | 中等 | 模拟 + 溢出边界处理 | ✅ |
| 14 | 最长公共前缀 | 简单 | 纵向 / 横向扫描 | ✅ |
| 58 | 最后一个单词的长度 | 简单 | 从右向左扫描（跳过尾随空格 + 数单词，O(1) 空间） | ✅ |
| 13 | 罗马数字转整数 | 简单 | 哈希 + 贪心（左小右大则减） | ✅ |
| 12 | 整数转罗马数字 | 中等 | 贪心配面值（13 面值对找零钱） | ✅ |
| 468 | 验证 IP 地址 | 中等 | 分段 + 规则校验 | ✅ |
| 65 | 有效数字 | 困难 | DFA 状态机（9 状态表驱动，承接 8 atoi 的 DFA 延伸） | ✅ |
| 1 | 两数之和 | 简单 | 哈希表一次遍历 | ✅ |
| 3 | 无重复字符的最长子串 | 中等 | 滑动窗口 + 哈希 | ✅ |
| 159 | 至多包含两个不同字符的最长子串 | 中等 | 滑动窗口 + 频率表（3 的 K=2 泛化，种类 >2 收缩 left，340 的特例） | ✅ |
| 5 | 最长回文子串 | 中等 | 中心扩展 / Manacher | ✅ |
| 15 | 三数之和 | 中等 | 排序 + 双指针 | ✅ |
| 16 | 最接近的三数之和 | 中等 | 排序 + 双指针维护最近和（k-sum 最近型，命中即返回，无需去重） | ✅ |
| 18 | 四数之和 | 中等 | 排序 + 固定两个 + 双指针（k-sum 模板） | ✅ |
| 11 | 盛最多水的容器 | 中等 | 双指针 | ✅ |
| 42 | 接雨水 | 困难 | 双指针 / 单调栈 | ✅ |
| 53 | 最大子数组和 | 中等 | Kadane DP / 前缀和 | ✅ |
| 56 | 合并区间 | 中等 | 排序 + 贪心 | ✅ |
| 57 | 插入区间 | 中等 | 区间插入 + 三阶段扫描 | ✅ |
| 121 | 买卖股票的最佳时机 | 简单 | 一次遍历 / DP | ✅ |
| 128 | 最长连续序列 | 中等 | 哈希集合 | ✅ |
| 76 | 最小覆盖子串 | 困难 | 滑动窗口 | ✅ |
| 30 | 串联所有单词的子串 | 困难 | 单词级定长滑窗（等长切分 + w 个 offset 分类 + 词频不变式） | ✅ |
| 45 | 跳跃游戏 II | 中等 | 贪心 | ✅ |
| 209 | 长度最小的子数组 | 中等 | 滑动窗口（正数单调） | ✅ |
| 713 | 乘积小于K的子数组 | 中等 | 滑动窗口 + 计数(right−left+1) | ✅ |
| 220 | 存在重复元素 III | 困难 | 桶分桶 + 滑动窗口（w=t+1 切值域，同桶必中/邻桶待查/远桶跳过，承接 219 等值查重升级为近似查重） | ✅ |
| 1248 | 统计「优美子数组」 | 中等 | 「至多 K」转化（exactly = atMost(K)−atMost(K−1)）/ 前缀和哈希 | ✅ |
| 435 | 无重叠区间 | 中等 | 贪心区间调度（按右端点） | ✅ |
| 974 | 和可被K整除的子数组 | 中等 | 前缀和 + 同余定理 | ✅ |
| 525 | 连续数组 | 中等 | 前缀和 + 哈希（0 当 −1 转换，存最早下标） | ✅ |
| 918 | 环形子数组的最大和 | 中等 | Kadane 环形变体（total − min_sum） | ✅ |
| 448 | 找到所有数组中消失的数字 | 简单 | 原地标记（下标当哈希键，取负编码存在性） | ✅ |
| 454 | 四数相加 II | 中等 | 分组 + 哈希（Meet in the Middle，四个独立数组只计数，对比 18 排序双指针） | ✅ |
| 334 | 递增的三元子序列 | 中等 | 贪心双阈值（维护 first/second，O(n) 判定 LIS≥3） | ✅ |
| 977 | 有序数组的平方 | 简单 | 对撞双指针（山谷形两端取大，从后往前填结果） | ✅ |
| 986 | 区间列表的交集 | 中等 | 双指针归并（有序区间列表取交集，结束早的先走） | ✅ |
| 315 | 计算右侧小于当前元素的个数 | 困难 | 归并排序 + 索引数组 / 树状数组（逆序对进阶，逐位置计数，承接 LCOF51） | ✅ |
| 395 | 至少有 K 个重复字符的最长子串 | 中等 | 分治（按 freq<k 字符切分）/ 滑动窗口（枚举窗口内字符种类数） | ✅ |
| 304 | 二维区域和检索 - 数组不可变 | 中等 | 二维前缀和 + 四角容斥（预处理 O(mn)、查询 O(1)） | ✅ |
| 392 | 判断子序列 | 简单 | 双指针贪心匹配 + 二分预处理批量查询（进阶 upper_bound） | ✅ |
| 611 | 有效三角形的个数 | 中等 | 排序 + 固定最大边 + 双指针计数（a+b&gt;c 区间计数，计数型双指针招牌题） | ✅ |
| 659 | 分割数组为连续子序列 | 中等 | 贪心 + 哈希计数（freq/tails 双表，续接优先 + 交换论证，最小堆通用版） | ✅ |
| 205 | 同构字符串 | 简单 | 双向哈希双射判定（s2t/t2s 双表同步核对，两方向单射合为双射；首现位置编码巧解，290/890 同模板） | ✅ |

#### 链表

| 题号 | 题目 | 难度 | 核心套路 | 状态 |
|------|------|------|----------|------|
| 2 | 两数相加 | 中等 | 模拟进位 | ✅ |
| 21 | 合并两个有序链表 | 简单 | 哑节点 + 双指针 | ✅ |
| 82 | 删除排序链表中的重复元素 II | 中等 | 哑节点 + 整段跳过重复组（83 进阶：全删 vs 留一） | ✅ |
| 86 | 分隔链表 | 中等 | 双子链尾插拼接（哑节点×2 稳定分区，尾插保序 + 断尾防环） | ✅ |
| 19 | 删除倒数第 N 个节点 | 中等 | 快慢双指针 | ✅ |
| 25 | K 个一组翻转链表 | 困难 | 分段翻转 | ✅ |
| 61 | 旋转链表 | 中等 | 成环再断开 | ✅ |
| 138 | 复制带随机指针的链表 | 中等 | 哈希 / 拼接拆分 | ✅ |
| 141 | 环形链表 | 简单 | 快慢指针 | ✅ |
| 142 | 环形链表 II | 中等 | 快慢指针找入口 | ✅ |
| 146 | LRU 缓存 | 中等 | 哈希 + 双向链表 | ✅ |
| 147 | 对链表进行插入排序 | 中等 | 哑节点 + 正向扫描插入（链表版插入排序，无前驱→从 dummy 起向后找位，对照 148 归并） | ✅ |
| 148 | 排序链表 | 中等 | 归并排序 | ✅ |
| 206 | 反转链表 | 简单 | 指针翻转 | ✅ |
| 237 | 删除链表中的节点 | 中等 | 值后移伪删除（无前驱时把删 node 偷换成删 node.next，O(1)/O(1)，前提非尾节点） | ✅ |

#### 树 / 二叉树

| 题号 | 题目 | 难度 | 核心套路 | 状态 |
|------|------|------|----------|------|
| 103 | 二叉树的锯齿形层序遍历 | 中等 | BFS + 奇偶层反向 | ✅ |
| 107 | 二叉树的层序遍历 II | 中等 | BFS 正序收集 + 末尾 reverse（102 自底向上变体，遍历与输出方向解耦） | ✅ |
| 109 | 有序链表转换二叉搜索树 | 中等 | 中序模拟（链表升序=中序序列，先递归左子树再填根值再递归右子树，O(n)/O(log n)，承接 108） | ✅ |
| 112 | 路径总和 | 简单 | DFS 判定（目标递减） | ✅ |
| 129 | 求根节点到叶节点数字之和 | 中等 | DFS 前缀累积 | ✅ |
| 156 | 上下翻转二叉树 | 中等 | 左脊游标时滞重连（prev/prev_right 慢一拍，自顶向下模拟自底向上翻转，原左子变新根/原根变新右子/原右子变新左子，原地 O(1) 空间） | ✅ |
| 222 | 完全二叉树的节点个数 | 简单 | 完全二叉树性质 + 二分 | ✅ |
| 662 | 二叉树最大宽度 | 中等 | BFS/DFS + 节点编号 | ✅ |
| 958 | 二叉树的完全性检验 | 中等 | 层序遍历判空节点 | ✅ |
| 94 | 中序遍历 | 简单 | 递归 / 栈迭代 / Morris | ✅ |
| 98 | 验证二叉搜索树 | 中等 | 中序单调性 | ✅ |
| 101 | 对称二叉树 | 简单 | 递归 / 队列 | ✅ |
| 102 | 层序遍历 | 中等 | BFS 队列 | ✅ |
| 104 | 二叉树最大深度 | 简单 | DFS / BFS | ✅ |
| 105 | 前序+中序构造二叉树 | 中等 | 递归分治 | ✅ |
| 114 | 二叉树展开为链表 | 中等 | 后序 / 迭代 | ✅ |
| 226 | 翻转二叉树 | 简单 | 递归 | ✅ |
| 236 | 二叉树最近公共祖先 | 中等 | 后序 DFS | ✅ |
| 297 | 二叉树序列化 | 困难 | 前序 + 队列 | ✅ |
| 450 | 删除二叉搜索树中的节点 | 中等 | BST 删除三情况 + 中序后继 | ✅ |
| 250 | 统计同值子树 | 中等 | 后序 DFS + 布尔返回值传递（子树同值性向上传递，true 时计数，空节点空真为真，叶子天然同值） | ✅ |

#### 动态规划

| 题号 | 题目 | 难度 | 核心套路 | 状态 |
|------|------|------|----------|------|
| 119 | 杨辉三角 II | 简单 | 单数组倒序递推（118 进阶：只返一行，j 从 i↓1 保证读旧写新，O(k) 空间） | ✅ |
| 118 | 杨辉三角 | 简单 | 递推模拟 | ✅ |
| 509 | 斐波那契数 | 简单 | 一维 DP 入门（剑指 Offer 10） | ✅ |
| 70 | 爬楼梯 | 简单 | 一维 DP | ✅ |
| 72 | 编辑距离 | 中等 | 二维 DP | ✅ |
| 120 | 三角形最小路径和 | 中等 | 自底向上网格 DP（倒序行 + 顺序列，零边界特判） | ✅ |
| 174 | 地下城游戏 | 困难 | 反向网格 DP（状态定义决定子结构，从终点倒推 + 哨兵消边界，承接 64/120） | ✅ |
| 198 | 打家劫舍 | 中等 | 一维 DP | ✅ |
| 213 | 打家劫舍 II | 中等 | 拆环为线 + 一维 DP | ✅ |
| 264 | 丑数 II | 中等 | 三指针多路归并 | ✅ |
| 300 | 最长递增子序列 | 中等 | DP / 二分 | ✅ |
| 322 | 零钱兑换 | 中等 | 完全背包 DP | ✅ |
| 343 | 整数拆分 | 中等 | 切分型 DP + 数学贪心（切成 3 最优，剑指 Offer 14） | ✅ |
| 416 | 分割等和子集 | 中等 | 0-1 背包 DP | ✅ |
| 32 | 最长有效括号 | 困难 | DP / 栈 | ✅ |
| 152 | 乘积最大子数组 | 中等 | 滚动 DP | ✅ |
| 1143 | 最长公共子序列 | 中等 | 二维 DP | ✅ |
| 718 | 最长重复子数组 | 中等 | 二维 DP（公共子串，不匹配归零 / 滑动窗口对齐 / 滚动哈希） | ✅ |
| 97 | 交错字符串 | 中等 | 二维布尔 DP（双源交错可行性，OR 二选一 + 滚动数组） | ✅ |
| 96 | 不同的二叉搜索树 | 中等 | 卡塔兰数计数 DP | ✅ |
| 877 | 石子游戏 | 中等 | 博弈型区间 DP（净赚 + Minimax） | ✅ |
| 10 | 正则表达式匹配 | 困难 | 二维 DP + `*` 两分支（匹配零次/再吃一次） | ✅ |
| 44 | 通配符匹配 | 困难 | 二维 DP + `*` 两分支（匹配空串/再吞一个），贪心双指针 O(1) 空间（承接 10） | ✅ |
| 132 | 分割回文串 II | 困难 | 预处理回文表 + 最小切割 DP（两阶段 DP） | ✅ |
| 377 | 组合总和 IV | 中等 | 完全背包求排列数（外层金额内层硬币，枚举末位） | ✅ |
| 474 | 一和零 | 中等 | 二维费用 0-1 背包（两维容量都倒序） | ✅ |
| 1049 | 最后一块石头的重量 II | 中等 | 0-1 背包求最值（相撞⇔赋±号，最小化两组差） | ✅ |
| 740 | 删除并获得点数 | 中等 | 值域建桶 + 打家劫舍 DP（问题转换：按值求和暴露相邻互斥） | ✅ |
| 526 | 优美的排列 | 中等 | 状态压缩 DP（bitmask 记已用集合，popcount 定位置，n≤15 甜区） | ✅ |
| 139 | 单词拆分 | 中等 | DP + 前缀缓存（dp[i]=前 i 字符能否拆，dp[j] 缓存命中 + s[j:i] 增量查 set，承接前缀复用思想） | ✅ |

#### 回溯

| 题号 | 题目 | 难度 | 核心套路 | 状态 |
|------|------|------|----------|------|
| 89 | 格雷编码 | 中等 | 镜像反射 + 公式 G(i)=i^(i&gt;&gt;1)（构造型，直接生成而非搜索） | ✅ |
| 93 | 复原 IP 地址 | 中等 | 回溯 + 分段合法性剪枝 | ✅ |
| 22 | 括号生成 | 中等 | 回溯剪枝 | ✅ |
| 39 | 组合总和 | 中等 | 回溯 | ✅ |
| 46 | 全排列 | 中等 | 回溯模板 | ✅ |
| 47 | 全排列 II | 中等 | 回溯 + 排序同层剪枝去重（!used[i-1] 剪重复分支） | ✅ |
| 77 | 组合 | 中等 | 回溯组合模板（start 索引 + 剪枝） | ✅ |
| 78 | 子集 | 中等 | 回溯 | ✅ |
| 79 | 单词搜索 | 中等 | DFS + 回溯 | ✅ |
| 131 | 分割回文串 | 中等 | 回溯 + 判断 | ✅ |
| 140 | 单词拆分 II | 困难 | 记忆化 DFS（dfs(start) 返回 s[start:] 全部句子，memo 缓存避免后缀重复搜索，dfs(n)=[""] 基准，139 的「枚举全部方案」版） | ✅ |
| 51 | N 皇后 | 困难 | 回溯 + 位运算 | ✅ |
| 37 | 解数独 | 困难 | 回溯 + 三表判重剪枝（承接 36） | ✅ |
| 254 | 因子的组合 | 中等 | 回溯 + 非递减因子（39 组合总和的乘法镜像，start 管去重，候选集动态枚举因子到 √remain，两分支：收尾记录 [i,remain/i] / 续拆 dfs(remain/i,i)） | ✅ |

#### 栈 / 队列 / 设计

| 题号 | 题目 | 难度 | 核心套路 | 状态 |
|------|------|------|----------|------|
| 232 | 用栈实现队列 | 简单 | 双栈倒换（摊还 O(1)） | ✅ |
| 150 | 逆波兰表达式求值 | 中等 | 操作数栈求值 | ✅ |
| 224 | 基本计算器 | 困难 | 栈处理括号与一元符号 | ✅ |
| 20 | 有效括号 | 简单 | 栈 | ✅ |
| 155 | 最小栈 | 中等 | 辅助栈 | ✅ |
| 84 | 柱状图最大矩形 | 困难 | 单调栈 | ✅ |
| 239 | 滑动窗口最大值 | 困难 | 单调队列 | ✅ |
| 739 | 每日温度 | 中等 | 单调栈 | ✅ |
| 901 | 股票价格跨度 | 中等 | 单调栈 + 跨度合并 | ✅ |
| 735 | 行星碰撞 | 中等 | 栈模拟碰撞（非单调栈） | ✅ |
| 946 | 验证栈序列 | 中等 | 栈模拟判定（能弹就弹贪心，剑指 Offer 31） | ✅ |
| 71 | 简化路径 | 中等 | 栈模拟路径层级（切分 + 压栈/弹栈，面试经典 150） | ✅ |
| 460 | LFU 缓存 | 困难 | 频率桶 + 哈希（freq→双向链表） | ✅ |
| 981 | 基于时间的键值存储 | 中等 | HashMap + 有序数组二分找右界（时间戳严格递增→天然有序） | ✅ |
| 767 | 重构字符串 | 中等 | 贪心 + 大根堆按频率交替放置 | ✅ |
| 502 | IPO | 中等 | 贪心 + 大顶堆（排序解锁 + 每轮取最大利润） | ✅ |
| 692 | 前 K 个高频单词 | 中等 | Top-K 最小堆 + 自定义比较器（堆顶=最差：频率↑同频字典序↓，方向与排序相反） | ✅ |

#### 二分查找 / 二分答案

| 题号 | 题目 | 难度 | 核心套路 | 状态 |
|------|------|------|----------|------|
| 704 | 二分查找 | 简单 | 闭区间模板（一切二分的母题） | ✅ |
| 69 | x 的平方根 | 简单 | 二分答案 | ✅ |
| 35 | 搜索插入位置 | 简单 | 二分找下界 | ✅ |
| 74 | 搜索二维矩阵 | 中等 | 二维一维化二分 | ✅ |
| 153 | 寻找旋转排序数组中的最小值 | 中等 | 二分缩区间（与右端点比较） | ✅ |
| 875 | 爱吃香蕉的珂珂 | 中等 | 二分答案 + O(n) 验证 | ✅ |
| 1011 | 在 D 天内送达包裹的能力 | 中等 | 二分答案 + 贪心验证 | ✅ |
| 410 | 分割数组的最大值 | 困难 | 二分答案 + 贪心划分 | ✅ |
| 719 | 找出第 K 小的数对距离 | 困难 | 二分答案 + 双指针计数 | ✅ |
| 378 | 有序矩阵中第K小的元素 | 中等 | 二分值域 + 左下角计数 / 小顶堆 k 路归并 | ✅ |

#### 图 / 搜索

| 题号 | 题目 | 难度 | 核心套路 | 状态 |
|------|------|------|----------|------|
| 200 | 岛屿数量 | 中等 | DFS / BFS / 并查集 | ✅ |
| 547 | 省份数量 | 中等 | 并查集模板 | ✅ |
| 684 | 冗余连接 | 中等 | 并查集判环（union 失败即冗余边） | ✅ |
| 207 | 课程表 | 中等 | 拓扑排序 / BFS | ✅ |
| 994 | 腐烂的橘子 | 中等 | 多源 BFS | ✅ |
| 743 | 网络延迟时间 | 中等 | 堆优化 Dijkstra 单源最短路 | ✅ |
| 787 | K 站中转内最便宜的航班 | 中等 | 限制松弛 Bellman-Ford（k+1 轮 + prev 快照） | ✅ |
| 79 | 单词搜索 | 中等 | DFS 回溯 | ✅ |
| 208 | 实现 Trie | 中等 | 字典树 | ✅ |
| 212 | 单词搜索 II | 困难 | Trie + DFS 回溯 | ✅ |
| 785 | 判断二分图 | 中等 | 二着色 BFS/DFS | ✅ |
| 417 | 太平洋大西洋水流问题 | 中等 | 反向多源 DFS/BFS（从海洋边界逆流而上，取两片海可达集交集） | ✅ |
| 1584 | 连接所有点的最小费用 | 中等 | Prim 最小生成树（堆优化 / 朴素 O(n²)） | ✅ |
| 332 | 重新安排行程 | 困难 | 有向图欧拉路径 Hierholzer（最小堆 + 后序入栈 + 逆序） | ✅ |

#### 数学 / 位运算

| 题号 | 题目 | 难度 | 核心套路 | 状态 |
|------|------|------|----------|------|
| 9 | 回文数 | 简单 | 反转一半数字（O(1) 空间规避溢出，承接 7 整数反转） | ✅ |
| 470 | 用 Rand7() 实现 Rand10() | 中等 | 拒绝采样（Rand49 → 取模） | ✅ |
| 398 | 随机数索引 | 中等 | 蓄水池抽样 R 算法（k=1，1/c 替换） | ✅ |
| 528 | 按权重随机选择 | 中等 | 加权随机采样（前缀和 + 二分找首个 > r） | ✅ |
| 191 | 位 1 的个数 | 简单 | n & (n−1) 消最低位 1（剑指 Offer 15） | ✅ |
| 231 | 2 的幂 | 简单 | n & (n−1) == 0 判单 1（Brian Kernighan 技巧判 2 的幂，承接 191 数 1） | ✅ |
| 371 | 两整数之和 | 中等 | 位运算模拟加法（剑指 Offer 65） | ✅ |
| 29 | 两数相除 | 中等 | 位运算模拟除法（左移翻倍成倍减，承接 371 加法 / 50 快速幂） | ✅ |
| 187 | 重复的DNA序列 | 中等 | 2 位编码 + 滚动哈希（4 碱基=2 bit，10-mer=20 bit 完美哈希双射，`<<2|code&MASK` 滚动 O(1)） | ✅ |
| 166 | 分数到小数 | 中等 | 长除法模拟 + 哈希检测循环节 | ✅ |
| 168 | Excel表列名称 | 中等 | 1-indexed 进制转换（先减 1 再取余） | ✅ |
| 171 | Excel表列序号 | 简单 | 1-indexed 进制转换合成方向（Horner 累乘加，无需减 1） | ✅ |
| 172 | 阶乘后的零 | 中等 | 勒让德公式 v₅(n!)=Σ⌊n/5ⁱ⌋（末尾零=2×5 配对数，2 恒过剩故数 5，迭代除 5 累加，O(log n)） | ✅ |
| 204 | 计数质数 | 中等 | 埃氏筛（划倍数 + 从 i*i 起 + 只筛到 √n）/ 线性筛 | ✅ |
| 149 | 直线上最多的点数 | 困难 | 哈希表 + GCD 化简分数表示斜率（固定点按斜率分组，精确哈希键） | ✅ |
| 202 | 快乐数 | 简单 | 数位平方和序列 + Floyd 判圈（隐式链表判环，承接 141/287 判环三件套） | ✅ |
| 60 | 第k个排列 | 困难 | 康托尔展开解码（阶乘进制，(k−1)//fact 逐位定候选，承接 31/46 排列家族） | ✅ |

> 💡 **一句话总结**：选题的本质是「按题号顺序逐题补全，追求题号的连续覆盖」；1.2 标签索引仅供检索已写题解，不影响下一个要写的题号。下一个要写的题号见 1.3 清单首行。

### 1.3 待补全题号清单（按序号优先）

按「按序号补全」策略，从最小缺失题号开始逐题填写。下表列出各题号区间的覆盖情况与缺失题号，**补写时从表中第一个缺号开始**。

> 统计基准：`leetcode/solution/` 下已有 385 篇题解，题号范围 1–3013，共缺失 2628 题。统计脚本见本节末尾。

| 区间 | 已有 | 缺失 | 缺失题号（连续段用 a-b 表示） |
|------|------|------|-------------------------------|
| 0001-0100 | 100 | 0 | —（区间已全部补完） |
| 0101-0200 | 100 | 0 | —（区间已全部补完） |
| 0201-0300 | 72 | 28 | 265-273, 275-278, 280-282, 284-286, 288, 290-294, 296, 298-299 |
| 0301-0400 | 27 | 73 | 301-303, 305-306, 308, 310-311, 313-314, 317-321, 323-327, 330-331, 333, 335-336, 339-342, 344-346, 348, 350-353, 356-370, 372-376, 379, 381-391, 393, 396-397, 400 |
| 0401-0500 | 24 | 76 | 401, 403-405, 407-409, 411-412, 414, 418-420, 422-423, 425-429, 431-434, 436, 439-444, 446-447, 449, 451, 453, 455-459, 461-467, 469, 471-473, 475-493, 495, 497-500 |
| 0501-0600 | 15 | 85 | 501, 504-508, 510-515, 517, 519-524, 527, 529-537, 539, 541-542, 544-546, 548-558, 561-580, 582-600 |
| 0601-0700 | 9 | 91 | 601-610, 612-620, 622-646, 648-658, 660-661, 663-683, 685-691, 693-694, 696-697, 699-700 |
| 0701-0800 | 14 | 86 | 701-703, 705-712, 715-717, 720-734, 736-738, 741-742, 744-751, 753-762, 764-766, 768-784, 786, 788-800 |
| 0801-0900 | 5 | 95 | 801-869, 871-874, 878-888, 890-900 |
| 0901-1000 | 11 | 89 | 902-906, 908-911, 913-917, 919-945, 947-957, 959-973, 975-976, 978-980, 982-985, 987-993, 995-1000 |
| 1001-1100 | 3 | 97 | 1001-1007, 1009-1010, 1012-1048, 1050-1100 |
| 1101-1200 | 2 | 98 | 1101-1108, 1110-1142, 1144-1200 |
| 1201-1300 | 1 | 99 | 1201-1247, 1249-1300 |
| 1301-1400 | 0 | 100 | 1301-1400（整段未覆盖） |
| 1401-1500 | 0 | 100 | 1401-1500（整段未覆盖） |
| 1501-1584 | 1 | 83 | 1501-1583（整段未覆盖，仅 1584 已写） |

> **当前进度**：`0001-0100` 区间已全部补完（99 已补写 `0001-0100/99_恢复二叉搜索树.md`）。`0101-0200` 区间首缺 107 已补写 `0101-0200/107_二叉树的层序遍历II.md`（102 层序遍历的自底向上变体），次缺 109 已补写 `0101-0200/109_有序链表转换二叉搜索树.md`（108 的链表版，核心为中序模拟：链表升序=BST 中序序列，O(n)/O(log n)），再补 119 已补写 `0101-0200/119_杨辉三角II.md`（118 的进阶版，核心为单数组倒序递推：j 从 i↓1 保证读 row[j-1] 时仍是上一行旧值，O(k) 空间满足进阶要求），再补 125 已补写 `0101-0200/125_验证回文串.md`（字符串版回文判定，核心为左右双指针原地比较：跳过非字母数字、忽略大小写逐对配对，O(1) 空间满足进阶，承接 9 回文数的「反转一半」思想），再补 126 已补写 `0101-0200/126_单词接龙II.md`（127 单词接龙的「返回全部最短序列」版，核心为 BFS 建最短路径 DAG（children 映射）+ DFS 回溯枚举：层末统一标记 visited 保住同层多父共子，对比 127 的「发现即标记」只求长度），再补 140 已补写 `0101-0200/140_单词拆分II.md`（139 单词拆分的「返回所有拆分方案」版，核心为记忆化 DFS：`dfs(start)` 返回 `s[start:]` 的全部句子列表，`memo[start]` 缓存避免同一后缀重复搜索，对比 139 的 `dp[i]` 只缓存布尔——从「判定可行性」升级为「枚举全部方案」，`dfs(n)=[""]` 而非 `[]` 是最易踩的坑），再补 147 已补写 `0101-0200/147_对链表进行插入排序.md`（链表版插入排序，核心为哑节点 + 正向扫描找位：链表无前驱指针无法「从后向前」，改为从 dummy 起向后扫到首个 `prev.next.val ≥ cur.val` 的位置再两步改指针插入，先存 `next` 防丢链、用 `<` 保稳定性，O(n²)/O(1)，对照 148 排序链表的归并 O(n log n)），再补 154 已补写 `0101-0200/154_寻找旋转排序数组中的最小值II.md`（153 的含重复元素版，核心为「三路二分 + 退缩」：`nums[mid]==nums[right]` 时无法判断断点在哪半，单独处理 `right−−` 丢弃非唯一候选逐步消歧，最坏全相同时退化为 O(n)（信息论下界），把 153 的两路模板 `else` 拆成 `else if (>)` 与 `else`（即 `==`）即得本题，两题共用同一套骨架），再补 156 已补写 `0101-0200/156_上下翻转二叉树.md`（树指针原地重构题，核心为沿左脊下移 + 游标时滞重连：`prev` / `prev_right` 慢 `cur` 一拍，把上一层身份带到当前层，让自顶向下的指针操作模拟自底向上的翻转——原左子变新根、原根变新右子、原右子变新左子，迭代 O(n)/O(1) 空间，对照 226 翻转二叉树的「逐节点交换」与 114 展开为链表的「反向后序头插」），再补 157 已补写 `0101-0200/157_用Read4读取N个字符.md`（API 模拟题，核心为 4 字节中转区 + 按需拷贝：`read4` 每轮把最多 4 字符舀进局部 `buf4`，`need = min(got, n - total)` 用一行 `min` 同时守住「读多了」与「读少了」两端边界，双出口（`total==n` / `got<4`）覆盖读满与 EOF，单次调用中 `buf4` 多读的字符可丢弃，对照 158 多次调用版需把 `buf4` 升格为成员变量跨调用缓存），再补 158 已补写 `0101-0200/158_用Read4读取N个字符II.md`（157 的多次调用版，核心为在 157 骨架上加一层持久化：`buf4` / `buf4Ptr` / `buf4Cnt` 升格为成员变量，`read` 进入循环后先判 `buf4Ptr==buf4Cnt`——缓存有剩则不调 `read4` 直接从 `buf4[Ptr..Cnt)` 取、缓存空才调 `read4` 并重置 `Ptr=0`，`need = min(buf4Cnt - buf4Ptr, n - total)` 把 157 的 `got` 换成缓存剩余供给，EOF 判定从 `got<4` 收紧为 `buf4Cnt==0`（返回 1~3 时仍有数据须先消费完再退），最易踩坑点是缓存消费完必须重置 `Ptr=0` 否则 `read4` 从 `buf4[0]` 写入后下标错位越界读残留，是 `BufferedReader` / `fread` 等流式包装器用内部缓冲区消解底层粗粒度与上层细粒度不匹配的最小原型），再补 159 已补写 `0101-0200/159_至多包含两个不同字符的最长子串.md`（3 无重复字符的最长子串的 K=2 泛化版，核心为频率哈希表 + 种类计数：right 扩展入窗 `freq[c]++`、种类数 >2 时 left 逐个收缩 `freq[s[left]]--` 直到某字符归 0 踢出该种类，对比第 3 题能「最新下标跳跃」——本题允许同字符多次出现，必须频率归 0 才确认踢出，故收缩逐个移动而非跳跃，O(n)/O(|Σ|)，扩展给出 340 的 K 泛化模板与「最新下标」跳跃优化两种变体）。再补 161 已补写 `0101-0200/161_相隔为1的编辑距离.md`（72 编辑距离的「恰好 1 次」特化版，核心为定位首个分歧点后一招定胜负：先交换保证 `s` 不比 `t` 长，再分等长走替换分支比 `s[i+1:]==t[i+1:]`、`t` 多一走插入分支比 `s[i:]==t[i+1:]` 两路，长度差 >1 与 `s==t` 前置否决，循环无分歧走收尾 `m+1==n`，从 $O(mn)$ DP 降为 $O(n)$ 扫描，承接 72 编辑距离的判定版）。再补 163 已补写 `0101-0200/163_缺失的区间.md`（228 汇总区间的对偶题，核心为哨兵统一三处缝隙：`prev` 初值 `lower−1`、末尾虚拟 `upper+1`，把首部/中部/尾部缝隙统一成 `(prev, cur)` 相邻对判定——`cur−prev==2` 恰好缺 1 个输出 `"prev+1"`、`cur−prev>2` 缺一段输出 `"prev+1->cur-1"`，格式化逻辑与 228 完全一致、断点判据互为补集，O(n)/O(1)），再补 164 已补写 `0101-0200/164_最大间距.md`（线性时间求排序后相邻元素最大差值，核心为桶排序 + 鸽巢原理：n 个数有 n−1 个间距、总和恰为 max−min，故最大间距 ≥ (max−min)/(n−1) = bucket_size 下界，据此分桶后桶内任意两数差 < bucket_size ≤ 最大间距 → 桶内无需排序、每桶只存 min/max，最大间距必落在相邻非空桶之间 gap=bmin−prev_max，以「不排序只比桶间」突破比较排序 O(n log n) 下界，O(n)/O(n)，扩展给出基数排序通解作对照）。再补 170 已补写 `0101-0200/170_两数之和III-数据结构设计.md`（1 两数之和的数据结构化变体，核心为 add/find 的复杂度权衡：默认 HashMap 计数方案 add O(1) / find O(n)——遍历 map 每个 key 查 comp=value−key 是否存在，自配对陷阱 comp==key 时须额外判 map[key]≥2 防单个元素自配；find 频繁时可切换为预计算两数和方案 add O(n) / find O(1) 但空间 O(n²)，跷跷板效应体现「数据结构设计本质是复杂度权衡」，承接 1 的静态一次性查询升级为动态持久化）。再补 171 已补写 `0101-0200/171_Excel表列序号.md`（168 的逆问题——「字符串→整数」的合成方向，核心为 Horner 累乘加 `res = res×26 + (ord(ch)−ord('A')+1)` 单遍扫完，O(n)/O(1)、无分支无反转；与 168 的关键对照：168 是分解（取余降位，数码须 0~25 对齐故每轮 `n−−`），171 是合成（累乘加，数码直接用 1~26），「分解怕 0 要平移、合成乘加随便填」是 1-indexed 进制两方向的统一规律；溢出上界 `"FXSHRXW"` = 2³¹−1 恰落 INT_MAX，res 单调递增保证中间值不溢出）。再补 172 已补写 `0101-0200/172_阶乘后的零.md`（数论计数题，核心为勒让德公式 v₅(n!) = Σ⌊n/5ⁱ⌋：末尾零由 2×5 配对数决定，2 恒过剩故数 5，用 `n //= 5; ans += n` 迭代累加——`n` 每轮除 5 后恰等于 ⌊n原/5ⁱ⌋，无需显式维护幂 5ⁱ，O(log₅ n)/O(1)，承接 171 的「单变量递推替代显式位权」思想；进阶 793 逆问题用本函数作二分单调判定器），再补 175 已补写 `0101-0200/175_组合两个表.md`（SQL JOIN 入门母题，核心为 LEFT JOIN：以 Person 为左表保留全部行、Address 为右表按 personId 匹配，无地址时 city/state 自动填 NULL，判断依据是「哪张表的行必须全部保留」→ 即 LEFT JOIN 的左表，对照 INNER JOIN 会丢弃无地址行、子查询无法展开一人多地址，提供 SQL + pandas merge 两种写法），再补 176 已补写 `0101-0200/176_第二高的薪水.md`（SQL「取第 N 高」家族入门款，承接 175 的 SQL 主线，核心为把「第二高」重述为「严格小于最高中的最大值」：内层 `MAX(salary)` 得第一高 M1，外层 `WHERE salary < M1` 排除全部最高（含重复、天然去重）再取 `MAX`，子集为空时 `MAX(∅)` 自动返回 NULL 行——聚合函数对空集返回一行 NULL 的语义是本题精髓，对比裸 `LIMIT OFFSET` 在无第二高时返回空结果集需外层 `SELECT(子查询)` 包装，提供 MAX 子查询 / DISTINCT+LIMIT 外包装 / DENSE_RANK 窗口函数三解，177 第 N 高是其直接泛化），再补 177 已补写 `0101-0200/177_第N高的薪水.md`（176 的直接泛化，核心为 OFFSET 预计算 + 函数封装：第 N 高 ⟺ `LIMIT 1 OFFSET N−1`，但 N 是参数、MySQL 的 OFFSET 不接受含运算符的表达式，须先 `SET N = N − 1` 求值为局部变量再用 `OFFSET N` 绕过；沿用 176 的「空集→NULL 行」外层 `SELECT (子查询)` 包装；解法 B 用 `DENSE_RANK() ... WHERE rk = N` 显式表达并列名次语义，无需 SET 但嵌套更深；N=1 退化为 MAX、N=2 退化到 176，三块拼图（去重 / 空集包装 / OFFSET 预计算）拼出任意第 N 高），再补 178 已补写 `0101-0200/178_分数排名.md`（SQL 排名函数的教学母题，核心为 DENSE_RANK 窗口函数：题目描述的三条规则——①从高到低、②同分同名次、③名次不跳号——逐字对应 `DENSE_RANK() OVER (ORDER BY score DESC)` 的语义定义，一行 SQL 即为最优解 O(n log n)；对照相关子查询 `COUNT(DISTINCT) WHERE s2.score >= s1.score` 的 O(n²) 经典写法，并通过 ROW_NUMBER / RANK / DENSE_RANK 三函数对比图阐明「数行跳号 vs 数不同值不跳号」的本质区别，承接 177 第N高薪水的 DENSE_RANK 选型与 176 第二高薪水的排名基础）。再补 180 已补写 `0101-0200/180_连续出现的数字.md`（SQL 自连接母题，核心为把「连续三行 num 相同」的跨行关系转为同行三列比较：自连接 `l1 JOIN l2 ON l2.id=l1.id+1 JOIN l3 ON l3.id=l1.id+2` + `WHERE l1.num=l2.num AND l2.num=l3.num` 错位对齐三表副本，或窗口函数 `LAG(num,1)/LAG(num,2) OVER(ORDER BY id)` 把前两行 num 拉到当前行，外层 WHERE 比三列相等——本质都是「跨行→同行」的转换思维，DISTINCT 去重防同一 num 多段三连，前两行 prev 为 NULL 由 SQL 三值逻辑自动过滤无需 COALESCE，扩展到「连续 N 次」用分组计数法（断点前缀和 + GROUP BY 段号 HAVING COUNT≥N，岛屿问题标准套路），承接 178 分数排名的窗口函数家族与 175/176/177 的 SQL 基础系列）。再补 181 已补写 `0101-0200/181_超过经理收入的员工.md`（SQL 自连接 Self JOIN 母题，核心为同一张表取两个别名 e（员工）/ m（经理）按自引用外键 `e.managerId = m.id` 连接——把「员工行」与「经理行」横向并排，`WHERE e.salary > m.salary` 同行比较两列即把跨行比较翻译成同行列比较；`managerId` 为 NULL 的顶层管理者因 `NULL = m.id` 在 SQL 三值逻辑下为 UNKNOWN 被 INNER JOIN 自动丢弃，无需手写 `WHERE managerId IS NOT NULL`，对照 175 用 LEFT JOIN 保留无匹配行、本题用 INNER JOIN 丢弃无匹配行——JOIN 类型取决于「无匹配行要不要留」；另有相关子查询等价解法但受标量约束且需优化器解相关，面试首推 Self JOIN；扩展到多层组织架构需递归 CTE 沿 `managerId` 链向上爬，承接 180 连续行的 `l1.id = l2.id + 1` 自连接与 196 重复邮箱的 DELETE 自连接，共同模式是「同表两副本 + ON 条件表达行间关系」）。再补 182 已补写 `0101-0200/182_查找重复的电子邮箱.md`（SQL 查重母题，核心为 `GROUP BY email HAVING COUNT(id) > 1`：把「逐行计数」的过程式思路转为「按值分组 + 对组过滤」的集合式思路，HAVING 是组级过滤（WHERE 是行级过滤，不可引用聚合函数），给出三种解法对比——GROUP BY + HAVING（推荐，天然去重无需 DISTINCT）、Self JOIN（`p1.email=p2.email AND p1.id<>p2.id`，N 行产生 N(N−1) 有向对须 DISTINCT，承接 181 自连接）、窗口函数 `COUNT OVER (PARTITION BY email)`（保留明细行不压缩，泛化到「至少 N 次」改比较符即可，承接 180 窗口语义），泛化范式 `HAVING COUNT(*)>=N` / `GROUP BY 多列` 覆盖所有查重 SQL 题，对照 196 删除重复邮箱的 DELETE 版），再补 183 已补写 `0101-0200/183_从不订购的客户.md`（SQL 反连接母题，核心为 LEFT JOIN + IS NULL：外连接保留左表全部行、无匹配补 NULL，`WHERE o.id IS NULL` 把「从未下单」显式化为「连接失败」标记，判右表主键 `o.id`（一定非 NULL）最稳；对比 NOT IN 子查询（集合差，⚠ 子查询含 NULL 则全返回空的三值逻辑陷阱）与 NOT EXISTS 相关子查询（NULL 安全，只判有无行不比较值），三者语义等价、现代优化器常统一为 Anti-Join）。再补 184 已补写 `0101-0200/184_部门工资最高的员工.md`（SQL「分组求最值」母题，核心为分组聚合与行筛选解耦：子查询 `GROUP BY depId, MAX(salary)` 算组级最大值，外层 `WHERE (depId, salary) IN 子查询` 用元组 IN 双键整体匹配把组级 MAX 回填到行级——元组 IN 严防跨部门串值（单标量 IN 会误选「A 部门中工资恰等于 B 部门最高」的员工），并列者元组相同全保留；对比窗口函数 `RANK() OVER (PARTITION BY ...)` 取 `rk=1`（扩展 Top-K 最优雅，承接 185 用 `DENSE_RANK <= 3`）与 LEFT JOIN 自连接（找「同部门无更高者」，承接 183 反连接思想，`ON e.salary < e2.salary` 须严格小于保并列）。再补 185 已补写 `0101-0200/185_部门工资前三高的所有员工.md`（184 的 Top-K 进阶版，核心为「分组 Top-K = `PARTITION BY departmentId` + `DENSE_RANK() OVER (ORDER BY salary DESC)` + 外层 `WHERE rk <= 3`」：把 184 的 `RANK() = 1` 改函数名 + 放阈值即得，但 Top-K 下函数选型从「三者皆可」收紧为「必须 DENSE_RANK」——`RANK` 并列后跳号会把第三档推到 rk=4 被 `WHERE rk<=3` 漏掉、`ROW_NUMBER` 给同薪者分不同号砍并列，只有 `DENSE_RANK`「按不同值计数、不跳号」正确表达「前 3 档不同工资」；窗口函数晚于 WHERE 故需套子查询后在外层过滤；相关子查询等价版 `COUNT(DISTINCT 同部门更大值) < 3` 是 DENSE_RANK 的无窗口函数表达，承接 184 解法 C 的「数严格更大者」思路（184 阈值=0，185 阈值=K-1），`DISTINCT` 与 `>` 不可省）。再补 186 已补写 `0101-0200/186_反转字符串中的单词II.md`（151 的原地核心子集，核心为两次翻转法：整体翻 reverse(0,n) + 逐词翻 reverse(每个单词)，字母级翻转两次抵消、单词级翻转保留，输入保证无多余空格故省去 151 的压缩空格步骤，纯考察 O(1) 原地实现；循环 i 取到 n 处理末尾单词是高频陷阱，翻转顺序可交换——先整体后逐词与先逐词后整体等价）。再补 187 已补写 `0101-0200/187_重复的DNA序列.md`（位运算压缩 + 滚动哈希招牌题，核心为 2 位编码：4 种碱基 A/C/G/T 恰需 2 bit，10 个碱基 = 20 bit 塞进一个 int，构成完美哈希双射无冲突（2^20 = 4^10），滚动公式 `hash = ((hash << 2) | code) & 0xFFFFF` 用左移腾位 + 掩码截断实现 O(1) 窗口推进，`cnt[hash] == 2` 首次重复时收答案天然去重，对照 1044 最长重复子串的 Rabin-Karp 通用版需处理冲突）。再补 190 已补写 `0101-0200/190_颠倒二进制位.md`（位运算入门母题，核心为逐位取放：`bit = n & 1` 取 n 最低位、`res = (res << 1) | bit` 左移腾位塞入新 bit、`n >>= 1` 消费 n 最低位，固定循环 32 次——位移对称性使最早取的 bit 被后续左移推到最高位、最后取的留在最低位，位序自然颠倒，必须固定 32 次而非 `while n`（高位 0 也要搬，对照 191 数 1 可跳 0 的 `while n` 恰相反）；进阶分治法用 5 步掩码交换（0x55/0x33/0x0F/0x00FF/0x0000FFFF）自底向上倍增翻转无循环 O(1)，承接 191 位 1 的个数的位运算双璧，对照 7 整数反转的十进制逐位取放同构），再补 192 已补写 `0101-0200/192_统计词频.md`（Shell 管道题，核心为 Unix 五段流水线：`awk` 拆词 → `sort` 聚类 → `uniq -c` 计数 → `sort -nr -k1` 按频降序 → `awk` 交换两列格式化，最大坑点是 `sort` 必须在 `uniq -c` 之前（后者只合并相邻重复行）且数值排序必带 `-n`，对照纯 awk 关联数组 `c[$i]++` 哈希一遍累加与 Python `Counter.most_common` 同构），再补 193 已补写 `0101-0200/193_有效电话号码.md`（Shell 正则题，核心为 ERE 一行正则精确描述两种电话号码格式——把「前缀二选一 + 公共后缀」提取公因式，前缀 A 为 `xxx-`、前缀 B 为 `(xxx) `（含尾空格，正则中括号须反斜杠转义），公共后缀 `xxx-xxxx`，整行用 `^` `$` 锚定后以 `grep -E` 逐行过滤；三处必踩坑：① 括号须转义否则被当分组元字符 ② 格式 B 的 `)` 后隐藏空格最易漏写 ③ `^` `$` 锚定整行不可省（`grep` 默认子串匹配，不锚则夹心行 `x987-123-4567y` 会漏过），对照 192 的「五段管道聚合统计」本题是「单正则模式过滤」——Unix 文本处理的两类范式），再补 194 已补写 `0101-0200/194_转置文件.md`（Shell 系列的矩阵转置题，核心为 awk 字段访问 `$i` + 关联数组 `s[i]` 列累加：逐行读入时把每行第 i 字段用空格拼到 `s[i]` 末尾，读完所有行后 `s[i]` 恰为输出第 i 行——「按列收集」与「按行累加」在转置下等价，首行 `s[i]=$i` 直接赋值避免 leading space 是最易踩的格式坑，`END` 块中 `NF` 保留末行值 = 列数 C，对照 192 的 `c[$i]++` 数值累加本题是字符串拼接累加——awk 关联数组的两类用法）。再补 195 已补写 `0101-0200/195_第十行.md`（Shell 系列的行寻址题，核心为用 Unix 工具的「行寻址」能力把「取第 N 行」压缩成一行命令——`awk 'NR==10'`（谓词过滤：NR 自增到 10 触发 print）、`sed -n '10p'`（地址 + 命令：`-n` 关默认输出 + `10` 行号地址 + `p` 显式打印）、`tail -n +10 | head -n 1`（范围截取：`+10` 从第 10 行起 + 取首行）三殊途同归，三者都以「第 10 行」为寻址目标故不足 10 行天然输出空；最大陷阱是 `head -n 10 | tail -n 1`：head 语义为「至多 10 行」不足则返回全部，把「下界」误当「上界」导致 5 行文件误输出第 5 行，修复为换成 `tail -n +10 | head -n 1` 起点寻址，对照 193 的「模式选行」与本题「行号选行」两个维度），再补 196 已补写 `0101-0200/196_删除重复的电子邮箱.md`（182 查找重复电子邮箱的 DELETE 版进阶，核心为 Self JOIN + `p1.id > p2.id` 单向配对：大 id 当 p1 待删、小 id 当 p2 存活，`>` 而非 `<>` 避免双向配对把存活者也删掉——从 182 的「组级查重」升级为「行级定位 + 存活者选择」，`DELETE p1 FROM Person p1 JOIN Person p2 ON ...` 一句搞定，对照 GROUP BY 子查询版需套派生表绕开 MySQL「不能在 DELETE 子查询中引用目标表」限制），再补 197 已补写 `0101-0200/197_上升温度.md`（全站首篇 SQL 题解，核心为自连接 + `DATEDIFF(w1.recordDate, w2.recordDate) = 1` 锚定「昨天」：题目要的是日历意义上的前一日而非「id 相邻的行」，`id−1` 在 id 缺口 / 日期缺口 / id 顺序与日期不一致时全错——必须用日期函数把「昨天」钉死在 `recordDate` 上；`w1.temperature > w2.temperature` 定方向，两步筛选缺一不可；附 LAG 窗口函数版与 Pandas `shift` 版作扩展，三者共用同一颗语义钉子——「日期差正好 1 天」：LAG 取的是「排序后上一行」而非「昨天」，日期有缺口时会错配到更早日期，故 `DATEDIFF=1` 不可省；跨方言用 `w2.recordDate = DATE_SUB(w1.recordDate, INTERVAL 1 DAY)` 等值匹配比 `DATEDIFF` 更可移植）。至此 `0101-0200` 区间 100 题全部补完。进入 `0201-0300` 区间，首缺 201 已补写 `0201-0300/201_数字范围按位与.md`（位运算 + 公共前缀招牌题，核心观察为 AND 结果 = `left` 与 `right` 二进制公共前缀 + 低位全 0：首个不同位 $k$ 处 `right−left ≥ 2^k` 使该位翻转必出现 0、更低位置翻转周期更短同样清零，只有从未翻转的高位保留；两种等价实现——位移法右移到相等再左移 `shift` 位复原最直观、Brian Kernighan 法 `right &= right−1` 消最低位 1 直到 `right ≤ left` 最简短，两法循环次数相等都为 $O(\log n)$/$O(1)$，复用 191 位 1 的个数的 `n & (n−1)` 技巧）。再补 203 已补写 `0201-0300/203_移除链表元素.md`（单链表删除指定值节点，核心为哑节点 + 单指针「跳过」：`curr` 从 `dummy` 出发始终盯住 `curr.next`，等于 `val` 则 `curr.next = curr.next.next` 脱链且 `curr` **不前进**——新的 `curr.next` 可能也等于 `val`（连续目标值如 7→7→7 须一次性全删，删后立刻前进会漏删，这是最易踩的坑），不等于 `val` 才 `curr` 前进；哑节点把「删头节点」统一成「删 dummy 的下一个」消除边界特例，最后返回 `dummy.next`，O(n)/O(1)，对照 83/82 删除排序链表中的重复元素的「留一/全删」家族与 19 删倒数第 N 个的哑节点统一删头；递归版 `head->next = removeElements(head->next, val)` 先清子链表再判 `head` 去留，从尾向头逐个决定，O(n) 栈空间）。再补 205 已补写 `0201-0300/205_同构字符串.md`（字符级双射判定招牌题，核心为双向哈希同步核对：`s2t` 与 `t2s` 两张表同步登记 + 核对——`s→t` 单射防「同源换不同目标」、`t→s` 单射防「不同源挤同一目标」，两个方向的单射合起来才是双射；⚠️ 单表只查 `s→t` 方向会漏掉 `t→s` 多对一（反例 `s="badc"`/`t="baba"` 误判 true），一次遍历 O(n)/O(|Σ|)；扩展给出「首次出现位置编码」巧解——把每个字符替换为它首次出现下标、两串的位置序列相等 ⇔ 同构，思想同 290 单词模式与 890 查找和替换模式，合称双射判定三件套），再补 211 已补写 `0201-0300/211_添加与搜索单词.md`（208 实现 Trie 的通配符版，核心为 Trie + DFS 分叉搜索：`addWord` 完全沿用 208 的标准插入，`search` 把 208 的迭代逐字符走升级为递归——普通字符走 `children[c]` 单分支、遇 `.` 枚举当前节点所有非空子节点并短路返回（任一命中即 true），递归终止判 `is_end` 而非仅路径存在，对照 208 的 `search` 只能走确定单链）。`0201-0300` 区间首缺 214 已补写 `0201-0300/214_最短回文串.md`（5 最长回文串的「只在前端补字符」变体，核心为 KMP 的 LPS 表妙用：构造 `s + "#" + reverse(s)` 跑一遍 LPS，末位值即 `s` 的最长回文前缀长度 L，答案 = `reverse(s)[:n-L] + s`——`#` 作防火墙截断跨段伪匹配保证 `lps[末位] ≤ n`，前缀 == 翻转后缀 ⟺ 回文是转化的关键，LPS 构建骨架与 28 题 strStr 逐行同构，O(n) 时间 O(n) 空间，对照暴力双指针判前缀回文 O(n²)）。进入 `0201-0300` 区间，首缺 216 已补写 `0201-0300/216_组合总和III.md`（77 组合与 39 组合总和的「合体」：候选固定 1-9、每个数用一次，骨架复用 77 的 `start+1` + 长度上界剪枝，叠加 39 的 `sum` 参数与 `sum+i>n → break` 和剪枝，收集条件升级为 `size==k 且 sum==n` 双判定，是组合题族「双重剪枝」思维的综合练手题，候选仅 9 个规模极小、重点在剪枝语义而非性能）。再补 217 已补写 `0201-0300/217_存在重复元素.md`（判定型去重入门题，核心为哈希集合做「已见过」账本：遍历到 `num` 先 `count` 查 `seen`、命中即返 `true`、未命中再 `insert`——**先查后加**保 `seen` 始终代表「严格在当前元素之前出现过的值」避免把自己误判为重复，O(n)/O(n)，命中越早越省（提前终止是优于排序法的最大优势）；扩展给出排序法 O(n log n)/O(1) 作空间受限场景的权衡（会修改输入且无法提前终止），对照 287 寻找重复数（禁排序+禁哈希+O(1) 空间逼出 Floyd 快慢指针判圈）、219/220 在 217 基础上叠加下标差/值差约束的递进家族）。再补 218 已补写 `0201-0300/218_天际线问题.md`（扫描线招牌困难题，核心为「建筑拆进入/离开事件 + 大顶堆维护活跃高度集合」：每栋 [L,R,H] 拆成 (L,−H) 进入与 (R,+H) 离开，按 (x,h) 升序一次排序自带三条同 x 拼接规则——都进入则高的先（堆顶立即抬高、矮的不影响）、都离开则矮的先（高的仍在、堆顶不掉）、一进一离则进入先（先加新高度再删旧高度避免瞬间跌 0 的假拐点）；堆中常驻哨兵 0 保证所有建筑离开后堆顶回到地面免空堆特判；按 x 分组处理完同 x 全部事件后查一次堆顶、与 prev 不同即输出关键点 [x,cur]，C++ 用 multiset（`erase(find(h))` 只删一个等于 h 的元素，避免 `erase(h)` 错杀同高建筑）、Python 无内置 multiset 改用大顶堆（存 −h）+ Counter 懒删除（`clean_top` 弹出堆顶死节点、每高度至多一进一出故摊还 O(log n)），O(n log n)/O(n)；扩展给出分治归并（合并两条已排序关键点序列取 max(hl,hr)，不依赖堆）与线段树（离散化 x + 区间取 max 更新）两种替代，对照 253 会议室II（同骨架堆维护活跃集合求最大重叠数）、732 我的日程安排表III（差分扫描线））。再补 219 已补写 `0201-0300/219_存在重复元素II.md`（217 的带距离约束版，核心为哈希表从「存值集合」升级为「值→最近下标」映射：贪心只记最近下标——更近的下标给出更小距离、更易满足 ≤k，覆盖旧的不会丢命中（最近的都不满足则更早的更不满足），查询时「在表且 i−last ≤ k」即命中返回 true、否则 `last[nums[i]]=i` 覆盖更新，O(n)/O(min(n,不同值数))；扩展给出滑动窗口法（窗口大小 k 的集合 + 满载踢出最老），k 小时空间 O(k) 更优，直接通向 220 桶分桶），再补 220 已补写 `0201-0300/220_存在重复元素III.md`（219 的值差约束升级版，核心为桶分桶 + 滑动窗口：桶大小 w=t+1 保证同桶两数差≤t（必中）、邻桶待查（验证|差|≤t）、远桶跳过；一桶一元素不变式保证正确性与空间 O(k)；C++ 负数桶号需 (x+1)/w−1 修正向下取整，Python // 天然向下取整无需处理），再补 223 已补写 `0201-0300/223_矩形面积.md`（几何容斥题，核心为容斥原理 + 一维区间交集：两轴对齐矩形并集面积 = |A|+|B|−|A∩B|，重叠矩形宽高各由 x、y 方向 `max(0, min(右端)−max(左端))` 给出——max(0,·) 把「不相交」吸收为「面积为 0」省去 if 分支，x/y 独立可拆因轴对齐矩形相交 = x 区间相交 且 y 区间相交；坐标 ±10^5 单矩形面积达 4×10^10 故 C++ 须先转 long long 再相乘，承接 836 矩形重叠的「只判相交」升级为「求相交面积」，对照 850 多矩形并集需扫描线 + 线段树），再补 225 已补写 `0201-0300/225_用队列实现栈.md`（232 用栈实现队列的对偶题，核心为单队列 + push 时旋转：push(x) 入队后立刻把前 size−1 个元素出队再入队（旋转）使 x 绕到队首，维持「队首=栈顶」不变量，push O(n)/pop·top·empty O(1)；与 232 最深刻的差异在于队列「搬运保序」无法像栈「倒换逆转」那样懒触发摊销——故 225 必须在 push 或 pop 之一付出 O(n)，无全操作摊还 O(1) 方案，扩展给出两队列的 push O(n)/pop O(1) 与 push O(1)/pop O(n) 两种等价实现）。再补 231 已补写 `0201-0300/231_2的幂.md`（位运算入门母题，核心为 `n > 0 && (n & (n-1)) == 0`：2 的幂二进制恰一个 `1`，Brian Kernighan 技巧抹掉它即归零，加 `n > 0` 排除 0 与负数，$O(1)$ 无循环满足进阶；承接 191 位 1 的个数——同一 `n & (n-1)` 技巧，191 用于「数 1」、231 用于「判 1 恰为 1」，扩展给出 `n & (-n) == n`（lowbit，树状数组同款）与 `(1<<30) % n == 0`（最大幂取模，对照 326 三的幂）两种等价写法）。`0201-0300` 区间首缺 233 已补写 `0201-0300/233_数字1的个数.md`（数位计数招牌题，核心为按位分解 + 分情况计数：把 n 在每位切成 `high/cur/low` 三段，按 `cur∈{0,1,≥2}` 三情况用 O(1) 算出该位为 1 的个数逐位累加，O(log n)/O(1)，承接 172 勒让德公式「按幂分层累加」同构——每个 1 唯一归属于某位、每个 5 因子归属于某层幂；`factor` 用 `long long` 规避 `factor*10` 在 10¹⁰ 处溢出 `int`；扩展给数位 DP 通用框架：`dfs(i,tight)` 返回 `(方案数, 1 的总个数)` 二元组，`ones += o + (w if d==1 else 0)` 把计数沿搜索树自底向上汇总，加 `tight/started` 状态维度即可适配任意位约束如 600 不含连续 1 / 902 数字集合限制）。再补 237 已补写 `0201-0300/237_删除链表中的节点.md`（链表删除的反直觉题，核心为「值后移法」伪删除：函数只给 `node` 不给 `head`，拿不到前驱无法标准脱链，于是把「删 `node`」偷换成「删 `node.next`」——`node.val = node.next.val` 让 `node` 借后继的值伪装、`node.next = node.next.next` 让 `node` 以「前驱身份」干掉真后继，两步 O(1)/O(1)，前提是 `node` 非尾节点（题目保证，否则无值可借、无节点可跳，只能 O(n) 找前驱但又不给 `head`，无解），本质是伪删除——被回收的是 `node.next` 的内存而 `node` 本身地址未变只换内容，节点带附加数据如 138 `random` 指针时只搬 `val` 会破坏指针语义须整体搬移，对照 203 握前驱脱链的标准姿势）。进入 `0201-0300` 区间，首缺 241 已补写 `0201-0300/241_为运算表达式设计优先级.md`（分治 + 记忆化招牌题，核心为枚举「最后执行的运算符」当树根：每个运算符轮流把表达式切成左右子串，`dfs(left) × dfs(right)` 笛卡尔积组合，纯数字子串即叶 `int(s)`，`memo` 把指数级重复展开压成 O(n²) 子问题；组合视角即卡塔兰递推 Cₙ=ΣC_{i-1}·C_{n-i}，承接 95/96 BST 家族与 22 括号生成的分治枚举全部方案同源，对照 224 基本计算器「给定括号求唯一值」与本题「枚举括号求所有值」对偶）。再补 242 已补写 `0201-0300/242_有效的字母异位词.md`（字母异位词家族的「单对判定」母题，核心为频次多重集判据：异位词 ⟺ 长度相同 + 各字符频次相同，小写字母用定长 cnt[26] 计数数组，s 计数 +1 / t 抵消 −1 共用一张表把「比对」融入「计数」，扫完校验全零即真，长度不同直接 false 既剪枝又堵住「按短串遍历漏字符」的 bug，O(n)/O(1)；承接 49 字母异位词分组的「签名」思想与 438 找到字符串中所有字母异位词的「滑窗增量更新」，进阶 Unicode 用哈希表退化为 O(k) 空间或排序 O(n log n)，并需 Unicode 规范化处理组合字符）。再补 243 已补写 `0201-0300/243_最短单词距离.md`（单词距离三件套的母题，核心为单遍扫描只保留最近位置：扫到 word1 时最近的 word2 一定是上一次见到的 p2，单调性保证更早的只会更远，故只需 p1/p2 两个游标，命中任一单词即与对方最近位置比距，O(n)/O(1)；`if/else if` 互斥分支体现 word1≠word2 约束、为 245 同单词版埋下区分点，扩展节给出「收集下标+双指针归并」的 O(n)/O(k) 通向 244 设计题版）。再补 244 已补写 `0201-0300/244_最短单词距离II.md`（243 的多次查询设计题版，核心为预存「单词→有序下标列表」哈希表 + 双指针归并求最小差值：构造 O(n) 建表、查询 O(k₁+k₂) 只扫两词位置列表，移动较小侧贪心保证不跳过最优对，用 O(n) 空间把 q 次查询从 O(qn) 降到 O(n+qk)，承接 243 第 5 节「收集下标+双指针归并」的伏笔，为 245 同单词版埋下区分点）。再补 245 已补写 `0201-0300/245_最短单词距离III.md`（243 的同单词版，核心为 `same = (word1 == word2)` 标志分流单遍扫描：同词时两游标合并为一个 `prev`，命中目标词「先算 `i − prev` 比距、后 `prev = i` 挪指针」保证两位置不同——避开 `i − i = 0` 的陷阱，对应暴力法里 `i == j` 跳过；异词时退化为 243 的 `if/else if` 双游标模板，扩展节剖析「双 `if` 陷阱」：同词时两个独立 `if` 会让 `p1`/`p2` 同设为 `i` 得到 0，揭示 `if/else if` 互斥语义依附于 `word1 ≠ word2` 前提、前提松动即崩坏，O(n)/O(1)，是三件套考验「分支语义与隐含前提」的收尾题），再补 246 已补写 `0201-0300/246_中心对称数.md`（带旋转映射的回文判定，核心为对撞双指针 + 旋转配对表：从两端向中间扫描，每对字符 `num[left]` 旋转 180° 后必须等于 `num[right]`，6↔9 互换、0/1/8 自对称，中间字符只能是自对称三数（双指针 `l<=r` 自然覆盖无需特判），非法字符 2/3/4/5/7 查表即 `false`，承接 9 回文数/125 验证回文串的对撞双指针家族——把「相等」换成「旋转后相等」即得本题，O(n)/O(1)，对照 247 中心对称数 II 的「生成」与 1056 易混淆数的「旋转有效但不等于原数」）。进入 `0201-0300` 区间，首缺 247 已补写 `0201-0300/247_中心对称数II.md`（246 中心对称数的「生成」版，核心为由外向内递归填对：每层从 5 个合法旋转映射中选一对包裹内层 `helper(m-2)` 的结果，最外层 `m==n` 时跳过 `0+s+0` 避免前导零，base case 为 `[""]`（偶数空核心）与 `["0","1","8"]`（奇数自对称单字符），时间 O(5^{n/2}·n) 正比于输出规模，与 22 括号生成同属「递归构造合法结构」家族），再补 248 已补写 `0201-0300/248_中心对称数III.md`（247 的计数版，核心为按长度生成 + 范围过滤：复用 247 的 `findStrobogrammatic`，外层套「遍历长度 len(low)..len(high) + 两条边界比较」，中间长度因位数决定大小可全部保留或闭式 f(L) 累加，仅边界长度生成比较，利用中心对称数稀疏性 5^{n/2}≪10^n 把暴力 O(10^H) 降到 O(5^{H/2}·H)，扩展给出闭式计数中间长度 + 数位 DP countLE 的 O(H²) 大规模解法，承接 246 判定 / 247 生成的「判定→生成→计数」三部曲），再补 249 已补写 `0201-0300/249_移位字符串分组.md`（[49 字母异位词分组](../0001-0100/49_字母异位词分组.md) 的「顺序相关」同构版，核心为规范化签名 + 哈希成组：把首字符归一到 'a'、其余字符跟随平移相同量（`(c−s[0]) mod 26`），消去移位量这一自由度后用哈希表自动成桶，O(n·L) 一次遍历；签名保留相对位置（差分元组为等价替代），对照 49 的「顺序无关」排序/频次签名，C++ 循环取模的负数修正 `+26` 是最易踩的坑），再补 250 已补写 `0201-0300/250_统计同值子树.md`（树形后序判定计数题，核心为「后序遍历 + 布尔返回值传递子结构结论」：`dfs(node)` 返回「该子树是否同值」，父节点据此与 `val == left.val / right.val` 一致性检查合流，一次遍历 $O(n)$ 完成判定与计数；空节点 `return true`（空真为真，vacuous truth）是关键约定——空子树不应拖累父节点，叶子天然同值无需特判；「布尔返回 + 副作用 `count++`」是树形判定计数题的通用模板，对照 124 二叉树最大路径和的后序传递子树贡献值）。再补 251 已补写 `0201-0300/251_展开二维向量.md`（设计迭代器展平二维向量，核心为双指针惰性遍历 + hasNext/next 职责分离：`hasNext()` 负责 `row++` 跳过空内层向量与行尾越界、把指针定位到下一个有效元素，`next()` 只管读取 `vec[row][col]` 并 `col++`；空行的 `size()==0` 使 `col=0>=0` 天然触发跳过，无需特判；均摊 O(1)/O(1)，对照展平法 O(N) 空间；进阶用 C++ 迭代器 `it/colIt/colEnd` 三件套替代下标，`advance()` 在构造时与每次读取后调用，与双指针版的「next 先调 hasNext」是同一思想的不同时序）。再补 252 已补写 `0201-0300/252_会议室.md`（253 会议室 II 的判定版，核心为排序后相邻比较：按 start 升序后，若相邻会议 `intervals[i][0] < intervals[i-1][1]` 即冲突返回 false，全无冲突返回 true；关键性质「相邻不重叠 ⇒ 与更早的也不重叠」把 O(n²) 两两判定压缩为 O(n) 扫描，半开区间用 `<` 而非 `<=`（[5,10] 与 [10,15] 不冲突），对照 253 的最小堆维护房间集合退化为单变量相邻比较，O(n log n)/O(log n)）。再补 254 已补写 `0201-0300/254_因子的组合.md`（[39. 组合总和](../0001-0100/39_组合总和.md) 的乘法镜像，核心为「回溯 + 非递减因子」：把 `n` 拆成若干因子之积，传 `start` 保证因子序列非递减以杜绝 `[2,6]` 与 `[6,2]` 重复——与 39 传 `start` 杜绝 `[2,3]` 与 `[3,2]` 重复完全同构，仅候选集从「给定数组」变为「对当前 remain 动态枚举 `[start, √remain]` 内的因子」；只枚举到 `√remain` 因因子成对出现取较小一半即覆盖所有非递减配对，把根层从 O(n) 压到 O(√n)；每发现因子 i 有两条分支——① 收尾记录 `[i, remain/i]`、② 续拆 `dfs(remain/i, i)`，缺一不可（只收尾漏 `[2,2,3]`、只续拆漏 `[2,6]`），递归传 `i` 而非 `i+1` 允许重复因子连选（同 39），O(√n + F(n)·log n)/O(log n)）。再补 255 已补写 `0201-0300/255_验证二叉搜索树的前序遍历序列.md`（1008 前序遍历构造 BST 的「验证版」，核心为单调栈追踪左脊 + 弹栈更新下界：栈维护从根到当前节点的祖先链（严格递减），遇比栈顶大的值说明「右拐」进入某祖先的右子树→弹栈定位该祖先→其值成为新下界 lower，此后任何 x < lower 即非法（出现在右子树中却比祖先小），下界单调递增是合法前序序列的本质不变式；与 739 每日温度/503 下一个更大元素 II 的单调栈骨架完全一致，弹栈语义从「记录距离」变为「验证合法性」；分治法 O(n²) 因每层线性找分区点且祖先信息不可复用，单调栈把祖先链显式存栈一次性弹出消解重复扫描降至 O(n)/O(n)，进阶 O(1) 空间复用输入数组前部作栈与 287 寻找重复数的数组原地复用异曲同工），再补 256 已补写 `0201-0300/256_粉刷房子.md`（线性 DP「相邻不同状态」模板，核心为状态 `dp[i][c]` = 涂完前 i 幢且第 i 幢涂 c 色的最小花费，转移 `dp[i][c] = cost[i][c] + min(dp[i−1][c′], c′≠c)`——K=3 固定故转移退化为「取另两色较小者」，三变量滚动数组 O(n)/O(1)，是 265 粉刷房子II 的 K 色推广（最小+次小压到 O(nK)）与 1473 三维 DP 的基础模板），再补 257 已补写 `0201-0300/257_二叉树的所有路径.md`（树 DFS 回溯的最纯净入门题，核心为共享 `path` 列表 + 选/撤销三步：进入 `append`、叶子处 `"->".join` 拼字符串收集、返回 `pop` 回溯，与 113 路径总和II 的回溯骨架完全同构，仅把「叶子且 `cur==target` 才收」退化为「叶子即收」的无过滤收集，对照 112 路径总和的 `||` 短路判定与 129 求根到叶数字之和的 `cur` 免回溯拼数；关键易错点为必须判叶子而非在 `node==null` 收集，否则一个叶子的两个空孩子会重复收两次；扩展给出字符串参数下传版，因 `str` 不可变天然免回溯，同 129 的 `cur×10+val` 免回溯理）。再补 258 已补写 `0201-0300/258_各位相加.md`（数字根入门题，核心为 mod 9 数学公式：10 ≡ 1 (mod 9) 推出数位和与原数同余，反复求和收敛到的一位数恰为 n mod 9（正数余 0 取 9），一行 `1 + (num - 1) % 9` 把 k 轮迭代压成一次取模，是「模拟→数学」优化的最纯净范例，弃九验算法的千年历史根基）。再补 261 已补写 `0201-0300/261_以图判树.md`（树判定题，核心为「树 ⟺ n−1 条边且连通」二条件合一：第一步 `edges.size() != n−1` 的 O(1) 边数预检一票否决边过多/过少，第二步并查集 `unite` 全部边后查 `count==1` 确认连通——边数恰 n−1 时连通与无环互为充要故无需单独判环，模板与 547 省份数量/684 冗余连接同源：547 数分量、684 抓冲突边、261 用分量数判连通；扩展给出纯并查集版把判环与判连通都交给并查集，对照两种分工）。再补 262 已补写 `0201-0300/262_行程和用户.md`（SQL 多表 JOIN + 条件聚合 Hard 母题，核心为「双 JOIN + AVG(CASE)」：Users 表 JOIN 两次起别名 uc/ud 分别查 client/driver 两端 banned 状态，WHERE 两端都 'No' 才保留，AVG(CASE WHEN status LIKE 'cancelled%' THEN 1 ELSE 0 END) 把取消率压缩成一次聚合——cancelled→1 / completed→0，AVG 天然是 1 的占比即取消数 / 总数，对照 180 连续出现的数字的自连接也是同表关联多次；扩展给出 NOT IN 子查询免 JOIN 版及其 NULL 陷阱、INNER JOIN vs LEFT JOIN 的天然过滤差异、varchar 日期的字典序 trick）。下一个要写的题号是 **263**（`0201-0300` 区间，下一个最小缺号）。259 已补写 `0201-0300/259_较小的三数之和.md`（15 三数之和的计数变体，核心为排序 + 双指针区间计数：固定 `nums[i]`，命中 `s<target` 时因有序性 `[left+1,right]` 全部命中，一次性 `count += right-left` 再 `left++`，把内层 O(n) 扫描压成 O(1) 加减法；与 15 的关键差异是按下标计数 `i<j<k` 故不去重——值相同下标不同也分别计，且不能照搬 15 的 `nums[i]>0 break` 剪枝因 target 可正可负，O(n²)/O(log n)），再补 263 已补写 `0201-0300/263_丑数.md`（264 丑数 II 的判定版基础，核心为试除剥光 2/3/5 看余数：三段 while 把 n 中所有 2/3/5 因子除尽，剩 1 即丑数、剩别的即有其他质因子，n≤0 直接 false、n=1 天然 true（三段 while 不触发 residue=1），因子两两互质故顺序无关，O(log n)/O(1)，对照 264 的三指针「生成」——逐个判定密度太低，求第 n 个须改用生成）。每写完一题后，从表中对应区间移除该题号；区间全部补完后进入下一区间。

> 另有 3 道无主站对应题的剑指 Offer 高频题：**礼物的最大价值**（LCR 166 / 剑指 Offer 47，网格 DP 求 max，✅ 已补写 `0101-0200/LCOF47_礼物的最大价值.md`，是 [64. 最小路径和](../0001-0100/64_最小路径和.md) 的 max 镜像，骨架完全一致仅 `min→max`）、**数组中的逆序对**（LCR 170 / 剑指 Offer 51，归并分治 / 树状数组，✅ 已补写 `0101-0200/LCOF51_数组中的逆序对.md`）、**圆圈中最后剩下的数字**（LCR 187 / 剑指 Offer 62，约瑟夫环递推，✅ 已补写 `0101-0200/LCOF62_圆圈中最后剩下的数字.md`）。它们不在主站题号体系内，按 LCOF 前缀归档到 LCR 题号对应区间目录，不参与序号补全。

**补写完成后同步三处**：

1. 本文件：1.3 表中对应区间移除已补题号，更新"已有/缺失"计数与"当前进度"行；
2. `leetcode/hot-interview.md`：若该题在汇总页中且题解列原为「—」，改为 `[站内题解](solution/<题号区间>/<题号>_<题名>.md)`；
3. `leetcode/8-week-plan.md`：若该题在 8 周计划表中，给题目补上 `（[题解](solution/<题号区间>/<题号>_<题名>.md)）`。

同步后重新 `python3 build.py`，汇总页与计划页即自动带上题解链接。

**统计脚本**（用于重新生成 1.3 表格，验证补全进度）：

```bash
cd leetcode/solution && python3 -c "
import os, re
nums = set()
for d in sorted(os.listdir('.')):
    if not os.path.isdir(d) or d == 'images': continue
    for f in os.listdir(d):
        m = re.match(r'(\d+)_', f)
        if m: nums.add(int(m.group(1)))
mx = max(nums)
def compact(missing):
    if not missing: return ''
    parts, s, p = [], missing[0], missing[0]
    for x in missing[1:]:
        if x == p+1: p = x
        else: parts.append(f'{s}-{p}' if s!=p else f'{s}'); s = p = x
    parts.append(f'{s}-{p}' if s!=p else f'{s}')
    return ', '.join(parts)
total_missing = 0
for lo in range(1, mx+1, 100):
    hi = min(lo+99, mx)
    missing = [i for i in range(lo, hi+1) if i not in nums]
    present = len([i for i in range(lo, hi+1) if i in nums])
    total_missing += len(missing)
    print(f'| {lo:04d}-{hi:04d} | {present} | {len(missing)} | {compact(missing)} |')
print(f'总计: 已有 {len(nums)}, 缺失 {total_missing}')
"
```

## 2. 目录组织

所有每日题解**必须写在** `leetcode/solution/` **下**，按**题号区间**组织（每 100 题一个目录），**不按难度分目录**。题目的「简单/中等/困难」仅作为元信息写在题解头部，不决定目录归属。

```
leetcode/solution/
├── 0001-0100/                     # 题号 1-100
│   ├── 1_两数之和.md              # 题解主体（文件名 = 题号_题目中文名）
│   ├── 42_接雨水.md
│   ├── 70_爬楼梯.md
│   └── ...
├── 0101-0200/                     # 题号 101-200
│   ├── 146_LRU缓存.md
│   ├── 200_岛屿数量.md
│   └── ...
├── 0201-0300/                     # 题号 201-300
│   ├── 206_反转链表.md
│   ├── 239_滑动窗口最大值.md
│   └── ...
├── ...
├── 1001-1100/                     # 题号 1001-1100
│   └── 1011_在D天内送达包裹的能力.md
├── 1101-1200/                     # 题号 1101-1200
│   └── 1143_最长公共子序列.md
├── images/                        # ⭐ 所有题解的 SVG 插图统一存放于此（全库共享）
└── SKILL.md                       # 本文件
```

**规则**：

1. **题解根目录**：`leetcode/solution/`，不要写到 `leetcode/` 根下或其它位置。
2. **区间目录**：按题号每 100 题一个目录，命名 `<起点>-<终点>`（4 位零填充）。题号 `N` 所属区间为 `((N-1)//100)*100+1` 到 `((N-1)//100)*100+100`。例：`1~100` → `0001-0100`，`101~200` → `0101-0200`，`1001~1100` → `1001-1100`，`1143` → `1101-1200`。区间目录内**只放 `.md` 题解**，不放图片。
3. **题解文件名**：`<题号>_<题目中文名>.md`（如 `42_接雨水.md`、`53_最大子数组和.md`），与文档一级标题一致。
4. **图片目录（统一）**：所有题解的 SVG 插图**统一存放**在 `leetcode/solution/images/`，**不在各区间目录下再建 `images/`**。图片文件名建议加题目缩写前缀避免重名冲突（如 `trap_water_column.svg`、`palindrome_partition_flow.svg`）。题解中引用用相对路径 `../images/<filename>.svg`（从区间目录上一级取）。
5. **难度不参与归类**：题目难度仅写在题解头部的 `**难度**：简单/中等/困难`，目录一律按题号区间归档，便于按号检索与补全。

> 💡 **选题 vs 归档**：第 1 节强调「按题号顺序补全」——这是**选题策略**（从 1.3 缺题清单的最小缺号开始写）；本节「按题号区间归档」是**存储结构**。二者天然一致：选题按序号、归档也按序号区间，写完一题自然落入对应区间目录。

## 3. 题解文档结构

每篇题解 `.md` 遵循固定章节顺序（参考 `0001-0100` 区间下的 `42_接雨水.md`、`53_最大子数组和.md`、`3_无重复字符的最长子串.md`）：

```markdown
# <题目中文名>

- **题目名称**：<题目中文名>
- **链接**：[<题号>. <题目名>](https://leetcode.cn/problems/<slug>/)
- **难度**：简单 / 中等 / 困难
- **标签**：<算法标签，逗号分隔>

## 1. 题目概述
（题意描述 + 示例 1/2/3 + 约束条件）

## 2. 解题思路
### 2.1 暴力思路
### 2.2 核心观察：<最优解法名>        ← 配 SVG 图
### 2.3 算法流程图                     ← 配 SVG 图
### 2.4 示例演算                       ← 配 SVG 图（可选但推荐）

## 3. 参考代码
### C++
### Python

## 4. 复杂度分析
（三列表格：维度 / 复杂度 / 说明）

## 5. 扩展：<进阶解法或变体>（可选）

## 6. 面试要点
（4-5 题 Q&A，问题加粗，答案缩进）

## 7. 同类练习题
（3-5 道同套路题目，列表格式：题号 + 题名链接 + 一句话关联说明）
```

**写作规范**：

- **中文为主**，概念加粗，善用 `> 💡` / `> ⚠️` blockquote。
- 代码块标注语言：` ```cpp` / ` ```python` / ` ```text`。
- 图片引用用相对路径：`![<中文alt>](../images/<filename>.svg)`（题解在区间目录如 `0001-0100/` 下，图片统一在上一级 `solution/images/`）。
- 每个 Day 平均引用 **2-4 张 SVG**。
- alt 文本用中文。
- **「同类练习题」**列出 3-5 道同套路题目，格式为 `- [题号. 题名](url)：一句话说明关联点`，帮助读者举一反三。

### 数学公式

- 行内公式用 `$...$`，块级公式用 `$$...$$`
- **禁止**用反引号 `` `...` `` 包裹数学公式，否则会被渲染为等宽代码，KaTeX 不会识别
- 公式内函数/运算符使用 LaTeX 命令：`\exp`、`\log`、`\sum`、`\max`、`\frac`、`\sqrt`，避免直接写 `exp`、`log`、`Σ`、`√`


## 4. 图片风格：手绘 sketch 风（Excalidraw-like）

**所有插图统一为手绘 sketch 风**，追求"轻松白板涂鸦"的直观感，避免冰冷完美的矢量感。具体要求：

- **禁止 ASCII 图片**：所有示意图、流程图、架构图一律用 SVG，不要在 Markdown 中嵌入 ASCII 字符画（如用 `+---+`、`|   |` 拼成的表格或流程图）

### 3.1 视觉特征

| 维度 | 要求 |
|------|------|
| **线条** | 手绘不均匀、略带抖动，避免完美直线或圆润矢量边 |
| **笔触** | 粗糙、类似马克笔/铅笔描边，线宽可略有变化 |
| **配色** | 极简，一般不超过 3-4 种柔和颜色（蓝 `#e8f0fe`/`#446688`、绿 `#e6f4ea`/`#4a7a3a`、橙 `#fff8e1`/`#d6a040`、红 `#fce4ec`/`#b85450`），背景白色或米白 `#fafafa` |
| **形状** | 简单几何块——矩形、网格、箭头、圆角框，不画复杂 3D 或写实元素 |
| **标签** | 手写感字体：英文用 `Comic Sans MS` / `Bradley Hand`，CJK 用 `Kaiti SC` / 楷体 |
| **整体感觉** | 轻松白板涂鸦，标注随意、轻微错位也无妨，优先可读性和直观性 |

### 3.2 SVG 实现技法（仓库统一用法）

用 SVG 滤镜 `feTurbulence` + `feDisplacementMap` 给所有图形叠加轻微抖动，实现手绘效果。**每张 SVG 顶部固定引入以下** `<defs>`：

```xml
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 520 360"
     font-family="'Comic Sans MS', 'Segoe UI', 'Kaiti SC', 楷体, cursive">
  <defs>
    <filter id="rough">
      <feTurbulence type="fractalNoise" baseFrequency="0.02" numOctaves="2" seed="3"/>
      <feDisplacementMap in="SourceGraphic" scale="2"/>
    </filter>
    <filter id="rough2">
      <feTurbulence type="fractalNoise" baseFrequency="0.025" numOctaves="2" seed="7"/>
      <feDisplacementMap in="SourceGraphic" scale="1.5"/>
    </filter>
  </defs>

  <rect width="520" height="360" fill="#fafafa"/>

  <!-- 所有矩形/路径/文本都加 filter="url(#rough2)" -->
  <rect x="80" y="48" width="50" height="30" fill="#e8f0fe"
        stroke="#446688" stroke-width="1.5" rx="4" filter="url(#rough2)"/>
  <text x="105" y="68" text-anchor="middle" font-size="12" fill="#333">−2</text>
  ...
</svg>
```

**要点**：

- `font-family` 必须包含 `'Comic Sans MS'` 和 `'Kaiti SC', 楷体`，保证中英文都有手写感。
- 每个图形元素（`rect`/`path`/`text`/`ellipse`/`polygon`）都加 `filter="url(#rough2)"`。
- 圆角矩形用 `rx="4"`（普通格子）或 `rx="8"`~`10`（说明框）。
- 强调元素用更粗的 `stroke-width`（2~2.5）和红色/橙色填充。
- 箭头用 `<marker id="arr">` 定义，`<path>` 引用 `marker-end="url(#arr)"`。

### 3.3 SVG 命名

全小写 + 下划线，语义化，建议加题目缩写前缀避免冲突：

- `trap_water_column.svg`、`trap_two_pointers.svg`（接雨水）
- `maxsubarray_kadane.svg`、`maxsubarray_algorithm_flow.svg`（最大子数组和）
- `lswrc_sliding_window.svg`、`lswrc_algorithm_flow.svg`、`lswrc_example_walkthrough.svg`（无重复字符的最长子串）

### 3.4 常见图类型

| 图类型 | 用途 | 示例 |
|--------|------|------|
| **概念图** | 直观展示核心观察（窗口、状态转移、水位） | `lswrc_sliding_window.svg` |
| **流程图** | 决策菱形 + 矩形框 + 箭头，展示算法步骤 | `lswrc_algorithm_flow.svg` |
| **演算表** | 逐步表格展示示例执行过程 | `lswrc_example_walkthrough.svg` |
| **对比图** | 多行格子对比数据变化（如 dp 数组） | `maxsubarray_kadane_v2.svg` |

## 5. 网站构建集成

题解写完后会被 `leetcode/build.py` 自动读取并生成网页：

- `build.py` 递归扫描 `leetcode/` 下所有 `.md`（排除 `public/`、`static/`、`images/`、`build/`、`.git` 等，并跳过 `SKILL.md`）。
- 解析一级标题 `# <title>` 作为侧边栏与列表页标题。
- **图片**：题解 SVG 统一存放在 `leetcode/solution/images/`。`build.py` 会把 `leetcode/images/`（顶层，供周赛等共用）与 `leetcode/solution/images/`（题解专用）一并复制到 `public/images/`。题解中引用的 `../images/xxx.svg` 在题解页（平铺于 `public/problems/`）原样保留，解析到 `public/images/xxx.svg`。
- **分组**：侧边栏「每日一题」与列表页均按**题号区间**分组（标签如 `1-100`、`101-200`…，由区间目录名生成），区间内按文件名（题号）排序。
- **重要**：新增 SVG 直接放入 `leetcode/solution/images/` 即可，构建会自动收集，无需再额外同步到其它目录。

**验证命令**（在 `leetcode/` 目录下运行）：

```bash
python3 build.py                     # 构建全站到 public/
```

**自检清单**：

- [ ] 题解位于 `leetcode/solution/<题号区间>/<题号>_<题目名>.md`，区间目录如 `0001-0100`
- [ ] 一级标题 `# <题目中文名>` 与文件名中题目名一致
- [ ] 含 2-4 张 SVG 插图，引用格式 `![中文alt](../images/xxx.svg)`，SVG 放在 `leetcode/solution/images/`
- [ ] SVG 为手绘 sketch 风（含 `feTurbulence` 抖动滤镜 + Comic Sans/Kaiti SC 字体）
- [ ] `python3 build.py` 成功生成对应 `public/problems/<slug>.html`
- [ ] `git push origin` 推送题解（commit + push 到远程）
